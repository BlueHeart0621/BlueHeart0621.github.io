<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>お前はどこまで見えている</title>
  
  <subtitle>僕たちは そうやって どこまで行くのだろう、どこまで行けるのだろう</subtitle>
  <link href="https://blueheart0621.github.io/atom.xml" rel="self"/>
  
  <link href="https://blueheart0621.github.io/"/>
  <updated>2021-04-28T03:45:37.096Z</updated>
  <id>https://blueheart0621.github.io/</id>
  
  <author>
    <name>BlueHeart0621</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Rust常见集合</title>
    <link href="https://blueheart0621.github.io/2021/04/22/Rust/Rust%E5%B8%B8%E8%A7%81%E9%9B%86%E5%90%88/"/>
    <id>https://blueheart0621.github.io/2021/04/22/Rust/Rust%E5%B8%B8%E8%A7%81%E9%9B%86%E5%90%88/</id>
    <published>2021-04-22T03:40:20.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-简介">1. 简介</h2><p>Rust 标准库中包含一系列被称为「集合」（collections）的非常有用的数据结构。不同于内建的数组和元组类型，这些集合指向的数据是储存在堆上的，这意味着数据的数量不必在编译时就已知，并且还可以随着程序的运行增长或缩小。</p><h2 id="2-向量（vector）">2. 向量（vector）</h2><p>vector 的数据类型为<code>Vec&lt;T&gt;</code>，它允许我们在一个单独的数据结构中储存多于一个的值，它在内存中彼此相邻地排列所有的值。</p><ul><li>vector 只能储存相同类型的值。</li></ul><h3 id="2-1-创建向量">2.1 创建向量</h3><p>创建一个新向量的基本语法示例如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 方式一：新建一个空的向量</span><br><span class="hljs-keyword">let</span> v: <span class="hljs-built_in">Vec</span>&lt;<span class="hljs-built_in">i32</span>&gt; = <span class="hljs-built_in">Vec</span>::new();<br><span class="hljs-comment">// 方式二：使用初始值来新建向量</span><br><span class="hljs-comment">// vec! 为 Rust 提供的一个宏</span><br><span class="hljs-keyword">let</span> v = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>];<br></code></pre></td></tr></table></figure><p>【注】在向量的结尾增加新元素时，在没有足够空间将所有所有元素依次相邻存放的情况下，可能会要求分配新内存并将老的元素拷贝到新的空间中。</p><h3 id="2-2-更新向量">2.2 更新向量</h3><ul><li>向一个向量末尾追加元素，可以使用 <code>push</code> 方法：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> v = <span class="hljs-built_in">Vec</span>::new();<br><span class="hljs-comment">// Rust 根据下面代码可以判断出向量的数据类型</span><br><span class="hljs-comment">// 故声明时可以不指定向量类型</span><br>v.push(<span class="hljs-number">5</span>);<br>v.push(<span class="hljs-number">6</span>);<br></code></pre></td></tr></table></figure><ul><li>向一个向量末尾移除元素，可以使用 <code>pop</code> 方法：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> v = <span class="hljs-built_in">Vec</span>::new();<br>v.push(<span class="hljs-number">5</span>);<br>v.push(<span class="hljs-number">6</span>);<br>v.pop();<br></code></pre></td></tr></table></figure><p>【注】要想能够更新向量，必须使用 <code>mut</code> 关键字使其可变。</p><h3 id="2-3-读取向量">2.3 读取向量</h3><p>有两种方法引用向量中储存的值：索引 <code>[]</code> 和 <code>get</code> 方法。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> v = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>];<br><br><span class="hljs-keyword">let</span> x = &amp;v[<span class="hljs-number">100</span>];<br><span class="hljs-keyword">let</span> y = v.get(<span class="hljs-number">100</span>);<br></code></pre></td></tr></table></figure><ul><li><code>get</code> 方法返回的是 <code>Option&lt;&amp;T&gt;</code> 类型。</li><li>使用 <code>[]</code> 方法时，当索引溢出，Rust 会 panic；使用 <code>get</code> 方法时，当索引溢出，Rust 不会 panic，相应地，其返回值为 <code>None</code> 值。</li></ul><h3 id="2-4-遍历向量">2.4 遍历向量</h3><p>可以使用 <code>for</code> 循环结构来遍历向量中的每一个元素：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> v = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">100</span>, <span class="hljs-number">32</span>, <span class="hljs-number">57</span>];<br><span class="hljs-keyword">for</span> i <span class="hljs-keyword">in</span> &amp;v &#123;<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, i);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="3-字符串（string）">3. 字符串（string）</h2><ul><li>Rust 的核心语言中只有一种字符串类型：<code>str</code>，即字符串 slice，它通常以被借用的形式出现：<code>&amp;str</code>。字符串 slice 是一些储存在别处的 UTF-8 编码字符串数据的引用。</li><li><code>String</code> 类型是由标准库提供的，而没有写进核心语言部分，它是可增长的、可变的、有所有权的、UTF-8 编码的字符串类型。</li><li>Rust 标准库中还包含一系列其他字符串类型，比如 <code>OsString</code>、<code>OsStr</code>、<code>CString</code> 和 <code>CStr</code>。</li></ul><p>【主】本文主要讨论的是标准库提供的 <code>String</code> 字符串。</p><h3 id="3-1-创建字符串">3.1 创建字符串</h3><p>创建一个新字符串的基本语法示例如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 方式一：新建一个空字符串</span><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> s = <span class="hljs-built_in">String</span>::new();<br><span class="hljs-comment">// 方式二：从字符串字面值创建字符串</span><br><span class="hljs-keyword">let</span> s = <span class="hljs-string">&quot;initial contents&quot;</span>.to_string();<br><span class="hljs-keyword">let</span> s = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;initial contents&quot;</span>);<br></code></pre></td></tr></table></figure><p>【注】字符串是 UTF-8 编码的，所以可以包含任何可以正确编码的数据。</p><h3 id="3-2-更新字符串">3.2 更新字符串</h3><ul><li>拼接两个 <code>String</code> 字符串可以直接使用 <code>+</code> 或 <code>format!</code> 宏来实现。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> s1 = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Hello, &quot;</span>);<br><span class="hljs-keyword">let</span> s2 = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;world!&quot;</span>);<br><span class="hljs-keyword">let</span> s3 = s1 + &amp;s2; <span class="hljs-comment">// 注意 s1 被移动了，不能继续使用</span><br></code></pre></td></tr></table></figure><p><code>+</code> 运算符使用了 <code>add</code> 函数，其函数签名如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">add</span></span>(<span class="hljs-keyword">self</span>, s: &amp;<span class="hljs-built_in">str</span>) -&gt; <span class="hljs-built_in">String</span> &#123;<br></code></pre></td></tr></table></figure><p>由 <code>add</code> 函数的 <code>s</code> 参数可知：只能将 <code>&amp;str</code> 和 <code>String</code> 相加，不能将两个 <code>String</code> 值相加，而且 <code>add</code> 获取了 <code>self</code> 的所有权。之所以能够在 <code>add</code> 调用中使用 <code>&amp;s2</code> 是因为 <code>&amp;String</code> 可以被强转成 <code>&amp;str</code>。<br />对于更为复杂的字符串拼接，可以使用 <code>format!</code> 宏，它返回一个带有结果内容的 <code>String</code>，并且不会获取任何参数的所有权。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> s1 = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;tic&quot;</span>);<br><span class="hljs-keyword">let</span> s2 = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;tac&quot;</span>);<br><span class="hljs-keyword">let</span> s3 = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;toe&quot;</span>);<br><br><span class="hljs-keyword">let</span> s = <span class="hljs-built_in">format!</span>(<span class="hljs-string">&quot;&#123;&#125;-&#123;&#125;-&#123;&#125;&quot;</span>, s1, s2, s3);<br></code></pre></td></tr></table></figure><ul><li>向一个字符串末尾追加字符，可以使用 <code>push_str</code> 方法。<code>push_str</code> 方法采用字符串 slice，因为并不需要获取参数的所有权。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> s = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;foo&quot;</span>);<br>s.push_str(<span class="hljs-string">&quot;bar&quot;</span>);<br></code></pre></td></tr></table></figure><h3 id="3-3-索引字符串">3.3 索引字符串</h3><p>Rust 的字符串不支持索引访问字符串字符。这是由于 <code>String</code> 采用 UTF-8 编码，而不同语言字符占用的字节数不同，因此 Rust 无法在常数时间内判断用户期待返回的字符占用的字节数及在字符串中对应的位置。<code>String</code> 是一个 <code>Vec&lt;u8&gt;</code> 的封装，本质上它存储的是一个个 <code>u8</code> 的数值，对字符串长度的计算即是 <code>Vec&lt;u8&gt;</code> 的长度，也就是字符串占用的字节数。</p><ul><li>虽然 Rust 不支持索引单个字符串，但可以使用 range 来创建包含特定字节的字符串 slice。需要注意的是，range 必须是一个合理的字符边界，即不能在多字节字符中间中断，否则 Rust 会 panic。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> hello = <span class="hljs-string">&quot;Здравствуйте&quot;</span>;<br><br><span class="hljs-keyword">let</span> s = &amp;hello[<span class="hljs-number">0</span>..<span class="hljs-number">4</span>];<span class="hljs-comment">// Зд</span><br></code></pre></td></tr></table></figure><h3 id="3-4-遍历字符串">3.4 遍历字符串</h3><ul><li>如果需要操作单独的 Unicode 标量值，可以使用 <code>chars</code> 方法：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">for</span> c <span class="hljs-keyword">in</span> <span class="hljs-string">&quot;नमस्ते&quot;</span>.chars() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, c);<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>如果需要返回每一个原始字节，可以使用 <code>bytes</code> 方法：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">for</span> b <span class="hljs-keyword">in</span> <span class="hljs-string">&quot;नमस्ते&quot;</span>.bytes() &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, b);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="4-哈希表（hash-map）">4. 哈希表（hash map）</h2><p><code>HashMap&lt;K, V&gt;</code> 类型储存了一个键类型 <code>K</code> 对应一个值类型 <code>V</code> 的映射。它通过一个哈希函数（hashing function）来实现映射，决定如何将键和值放入内存中。</p><ul><li>哈希表可以用于需要任何类型作为键来寻找数据的情况，而不是像数组那样通过索引。</li><li>类似于向量，哈希表也是同质的：所有的键必须是相同类型，值也必须都是相同类型。</li></ul><p>【注】在这三个常用集合中，<code>HashMap</code> 是最不常用的，所以并没有被 prelude 自动引用。</p><blockquote><p><strong>哈希函数</strong></p><ul><li>Rust 中的 <code>HashMap</code> 默认使用一种「密码学安全的」（“cryptographically strong” ）哈希函数，它可以抵抗拒绝服务（Denial of Service, DoS）攻击。</li><li>不过这并不是可用的最快的算法。如果性能监测显示此哈希函数非常慢，以致于你无法接受，你可以指定一个不同的 hasher 来切换为其它函数。hasher 是一个实现了 <code>BuildHasher</code> trait 的类型。</li></ul></blockquote><h3 id="4-1-创建哈希表">4.1 创建哈希表</h3><p>创建一个新哈希表的基本语法示例如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::HashMap;<br><span class="hljs-comment">// 方式一：新建一个空哈希表</span><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> scores = HashMap::new();<br><span class="hljs-comment">// 方式二：使用向量的 collect 方法</span><br><span class="hljs-comment">// 将两个向量按键值对转化为一个哈希表</span><br><span class="hljs-keyword">let</span> teams  = <span class="hljs-built_in">vec!</span>[<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Blue&quot;</span>), <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Yellow&quot;</span>)];<br><span class="hljs-keyword">let</span> initial_scores = <span class="hljs-built_in">vec!</span>[<span class="hljs-number">10</span>, <span class="hljs-number">50</span>];<br><br><span class="hljs-keyword">let</span> scores: HashMap&lt;_, _&gt; = teams.iter().zip(initial_scores.iter()).collect();<br></code></pre></td></tr></table></figure><p>【注】<code>_</code> 用于占位，Rust 能够根据向量中数据的类型推断出 <code>HashMap</code> 所包含的类型。</p><ul><li>对于像 <code>i32</code> 这样的实现了 <code>Copy</code> trait 的类型，其值可以拷贝进哈希表。</li><li>对于像 <code>String</code> 这样拥有所有权的值，其值将被移动而哈希表会成为这些值的所有者。</li></ul><h3 id="4-2-访问哈希表">4.2 访问哈希表</h3><ul><li>可以通过 <code>get</code> 方法并提供对应的键来从哈希表中获取值：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::HashMap;<br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> scores = HashMap::new();<br><br>scores.insert(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Blue&quot;</span>), <span class="hljs-number">10</span>);<br>scores.insert(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Yellow&quot;</span>), <span class="hljs-number">50</span>);<br><br><span class="hljs-keyword">let</span> team_name = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Blue&quot;</span>);<br><span class="hljs-keyword">let</span> score = scores.get(&amp;team_name);<br></code></pre></td></tr></table></figure><ul><li>可以使用与向量类似的方式来遍历哈希表中的每一个键值对，即 <code>for</code> 循环：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::HashMap;<br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> scores = HashMap::new();<br><br>scores.insert(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Blue&quot;</span>), <span class="hljs-number">10</span>);<br>scores.insert(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Yellow&quot;</span>), <span class="hljs-number">50</span>);<br><br><span class="hljs-keyword">for</span> (key, value) <span class="hljs-keyword">in</span> &amp;scores &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;: &#123;&#125;&quot;</span>, key, value);<br>&#125;<br></code></pre></td></tr></table></figure><h3 id="4-3-更新哈希表">4.3 更新哈希表</h3><ul><li>覆盖一个值：如果我们插入了一个键值对，接着用相同的键插入一个不同的值，与这个键相关联的旧值将被替换。</li><li>只在键没有对应值时插入：哈希表有一个特有的 API，叫做 <code>entry</code>，它获取我们想要检查的键作为参数。<code>entry</code> 函数的返回值是一个枚举 <code>Entry</code>，它代表了可能存在也可能不存在的值。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::HashMap;<br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> scores = HashMap::new();<br>scores.insert(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Blue&quot;</span>), <span class="hljs-number">10</span>);<br><br>scores.entry(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Yellow&quot;</span>)).or_insert(<span class="hljs-number">50</span>);<br>scores.entry(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;Blue&quot;</span>)).or_insert(<span class="hljs-number">50</span>);<br><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>, scores);<br></code></pre></td></tr></table></figure><p>其中，<code>Entry</code> 的 <code>or_insert</code> 方法在键对应的值存在时就返回这个值的可变引用，如果不存在则将参数作为新值插入并返回新值的可变引用。</p><ul><li>根据旧值更新一个值：另一个常见的哈希表的应用场景是找到一个键对应的值并根据旧的值更新它。比如统计一段文本中每个单词的出现数量：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::HashMap;<br><br><span class="hljs-keyword">let</span> text = <span class="hljs-string">&quot;hello world wonderful world&quot;</span>;<br><br><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> map = HashMap::new();<br><br><span class="hljs-keyword">for</span> word <span class="hljs-keyword">in</span> text.split_whitespace() &#123;<br>    <span class="hljs-keyword">let</span> count = map.entry(word).or_insert(<span class="hljs-number">0</span>);<br>    *count += <span class="hljs-number">1</span>;<br>&#125;<br><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;:?&#125;&quot;</span>, map);<br></code></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-简介&quot;&gt;1. 简介&lt;/h2&gt;
&lt;p&gt;Rust 标准库中包含一系列被称为「集合」（collections）的非常有用的数据结构。不同于内建的数组和元组类型，这些集合指向的数据是储存在堆上的，这意味着数据的数量不必在编译时就已知，并且还可以随着程序的运行增长或缩小</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust模块系统</title>
    <link href="https://blueheart0621.github.io/2021/04/21/Rust/Rust%E6%A8%A1%E5%9D%97%E7%B3%BB%E7%BB%9F/"/>
    <id>https://blueheart0621.github.io/2021/04/21/Rust/Rust%E6%A8%A1%E5%9D%97%E7%B3%BB%E7%BB%9F/</id>
    <published>2021-04-21T13:59:54.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-简介">1. 简介</h2><p>Rust 的模块系统用来管理代码的组织，包括哪些内容可以被公开，哪些内容作为私有部分，以及程序每个作用域中的名字等。模块系统包括：</p><ul><li>包（Packages）：Cargo 的一个功能，允许构建、测试和分享 crate。</li><li>crate：一个模块的树形结构，它形成了库或二进制项目。</li><li>模块（Modules）和 use： 允许你控制作用域和路径的私有性。</li><li>路径（path）：一个命名例如结构体、函数或模块等项的方式。</li></ul><h2 id="2-包和-crate">2. 包和 crate</h2><h3 id="2-1-crate">2.1 crate</h3><p>crate 是一个二进制项或者库。<code>crate root</code> 是一个源文件，Rust 编译器以它为起始点，并构成你的 crate 的根模块。</p><ul><li>一个 crate 会将一个作用域内的相关功能分组到一起，使得该功能可以很方便地在多个项目之间共享。</li></ul><h3 id="2-2-包">2.2 包</h3><p>包（package） 是提供一系列功能的一个或者多个 crate。一个包会包含有一个 <code>Cargo.toml</code> 文件，阐述如何去构建这些 crate。</p><ul><li>一个包中至多只能包含一个库 crate（library crate）。</li><li>包中可以包含任意多个二进制 crate（binary crate）。</li><li>包中至少包含一个 crate，无论是库的还是二进制的。</li></ul><blockquote><p>当我们使用 <code>cargo new &lt;project&gt;</code> 创建一个包的时候会发现，Cargo 创建的 <code>Cargo.toml</code> 文件中并没有提到 <code>src/main.rs</code>，这是因为 Cargo 遵循一个约定：</p><ul><li><code>src/main.rs</code> 就是一个与包同名的二进制 crate 的 <code>crate root</code>。同样的，Cargo 知道如果包目录中包含 <code>src/lib.rs</code>，则包带有与其同名的库 crate，且 <code>src/lib.rs</code> 是 <code>crate root</code>。crate 根文件将由 Cargo 传递给 <code>rustc</code> 来实际构建库或者二进制项目。</li><li>如果一个包同时含有 <code>src/main.rs</code> 和 <code>src/lib.rs</code>，则它有两个 crate：一个库和一个二进制项，且名字都与包相同。通过将文件放在 <code>src/bin</code> 目录下，一个包可以拥有多个二进制 crate，每个 <code>src/bin</code> 下的文件都会被编译成一个独立的二进制 crate。</li></ul></blockquote><h2 id="3-模块">3. 模块</h2><p>模块可以将一个 crate 中的代码进行分组，以提高可读性与重用性。模块还可以控制项的「私有性」，即项是可以被外部代码使用的（public），还是作为一个内部实现的内容，不能被外部代码使用（private）。</p><h3 id="3-1-定义">3.1 定义</h3><p>定义一个模块的基本语法格式如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> &lt;name&gt; &#123;<br>...<br>&#125;<br></code></pre></td></tr></table></figure><p>其中，<code>&lt;name&gt;</code> 为模块名。在模块内，还可以嵌套其他模块。通过使用模块，我们可以将相关的定义分组到一起，并指出他们为什么相关。</p><h3 id="3-2-模块树">3.2 模块树</h3><p>前面提到，<code>src/main.rs</code> 和 <code>src/lib.rs</code> 叫做 <code>crate root</code>。之所以这样叫它们是因为这两个文件的内容都分别在 crate 模块结构的根组成了一个名为 crate 的模块，该结构被称为「模块树」（module tree）。以以下模块为例：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> front_of_house &#123;<br>    <span class="hljs-keyword">mod</span> hosting &#123;<br>        <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">add_to_waitlist</span></span>() &#123;&#125;<br><br>        <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">seat_at_table</span></span>() &#123;&#125;<br>    &#125;<br><br>    <span class="hljs-keyword">mod</span> serving &#123;<br>        <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">take_order</span></span>() &#123;&#125;<br><br>        <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">server_order</span></span>() &#123;&#125;<br><br>        <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">take_payment</span></span>() &#123;&#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><p>其模块树结构为：</p><figure class="highlight mipsasm"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs mipsasm">crate<br> └── front_of_house<br>     ├── hosting<br>     │   ├── <span class="hljs-keyword">add_to_waitlist</span><br><span class="hljs-keyword"> </span>    │   └── seat_at_table<br>     └── serving<br>         ├── take_order<br>         ├── serve_order<br>         └── take_payment<br></code></pre></td></tr></table></figure><ul><li>Rust 中默认所有项（函数、方法、结构体、枚举、模块和常量）都是私有的。</li><li>父模块中的项不能使用子模块中的私有项，但是子模块中的项可以使用他们父模块中的项。这是因为子模块封装并隐藏了他们的实现详情，但是子模块可以看到他们定义的上下文。</li><li>可以通过使用 <code>pub</code> 关键字来创建公共项，使子模块的内部部分暴露给上级模块。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> front_of_house &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">mod</span> hosting &#123;<br>        <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">add_to_waitlist</span></span>() &#123;&#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><h4 id="将模块分割进不同文件">将模块分割进不同文件</h4><p>比如，将 <code>front_of_house</code> 模块移动到属于它自己的文件 <code>src/front_of_house.rs</code> 中：</p><ul><li>改变 crate 根文件 <code>src/lib.rs</code> 或 <code>src/main.rs</code>，通过声明 <code>front_of_house</code> 模块告诉 Rust 在另一个与模块同名的文件中加载模块的内容：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> front_of_house;<span class="hljs-comment">// 声明 front_of_house 模块</span><br></code></pre></td></tr></table></figure><ul><li>创建一个 <code>src/front_of_house</code> 目录和一个包含 <code>hosting</code> 模块定义的 <code>src/front_of_house/hosting.rs</code> 文件：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">pub</span> <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">add_to_waitlist</span></span>() &#123;&#125;<br></code></pre></td></tr></table></figure></blockquote><p>【注】Rust 中的私有性规则不但应用于模块，还应用于结构体、枚举、函数和方法。</p><blockquote><ul><li>如果我们在一个结构体定义的前面使用了 <code>pub</code>，这个结构体会变成公有的，但是这个结构体的字段仍然是私有的。如果要将字段公有化，则还需要分别在字段前使用 <code>pub</code> 关键字。</li><li>如果我们将枚举设为公有，则它的所有成员都将变为公有。</li></ul></blockquote><h3 id="3-3-路径">3.3 路径</h3><p>在 Rust 中要在模块树中找到一个项的位置，需要使用路径的方式，就像在文件系统使用路径一样。路径有两种形式：</p><ul><li>绝对路径（absolute path）：从 crate 根开始，以 crate 名或者字面值 <code>crate</code> 开头。</li><li>相对路径（relative path）：从当前模块开始，以 <code>self</code>、<code>super</code> 或当前模块的标识符开头。</li></ul><p>绝对路径和相对路径都后跟一个或多个由双冒号 <code>::</code> 分割的标识符。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> front_of_house &#123;<br>    <span class="hljs-keyword">mod</span> hosting &#123;<br>        <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">add_to_waitlist</span></span>() &#123;&#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">pub</span> <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">eat_at_restaurant</span></span>() &#123;<br>    <span class="hljs-comment">// 绝对路径</span><br>    crate::front_of_house::hosting::add_to_waitlist();<br><br>    <span class="hljs-comment">// 相对路径</span><br>    front_of_house::hosting::add_to_waitlist();<br>&#125;<br></code></pre></td></tr></table></figure><p>还可以使用 <code>super</code> 开头来构建从父模块开始的相对路径。类似于文件系统中以 <code>..</code> 开头的语法。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">serve_order</span></span>() &#123;&#125;<br><br><span class="hljs-keyword">mod</span> back_of_house &#123;<br>    <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">fix_incorrect_order</span></span>() &#123;<br>        cook_order();<br>        super::serve_order();<br>    &#125;<br><br>    <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">cook_order</span></span>() &#123;&#125;<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><h4 id="use-关键字引入路径"><code>use</code> 关键字引入路径</h4><p>对每一个模块中的函数都使用路径来调用非常冗长且重复，并不方便。Rust 提供了 <code>use</code> 关键字将路径一次性引入作用域，然后就可以直接调用该路径中的项，就如同它们是本地项一样。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> front_of_house &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">mod</span> hosting &#123;<br>        <span class="hljs-keyword">pub</span> <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">add_to_waitlist</span></span>() &#123;&#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">use</span> crate::front_of_house::hosting;<span class="hljs-comment">// 绝对路径</span><br><span class="hljs-comment">// use front_of_house::hosting;// 相对路径</span><br><br><span class="hljs-keyword">pub</span> <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">eat_at_restaurant</span></span>() &#123;<br>    hosting::add_to_waitlist();<br>    hosting::add_to_waitlist();<br>    hosting::add_to_waitlist();<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>作用域中使用 <code>use</code> 引入路径类似于在文件系统中创建软连接。</li><li>通过 <code>use</code> 引入作用域的路径也会检查私有性，同其它路径一样。</li><li>使用 <code>use</code> 引入函数时，习惯将函数的父模块引入到作用域，这样可以清晰地表明函数不是在本地定义的，同时使完整路径的重复度最小化。</li><li>使用 <code>use</code> 引入结构体、枚举和其他项时，习惯直接引入它们的完整路径。</li></ul><p>当需要引入很多定义于相同包或相同模块的项时，为每一项单独列出一行会占用源码很大的空间。相反，我们可以使用嵌套路径将相同的项在一行中引入作用域。这么做需要指定路径的相同部分，接着是两个冒号，接着是大括号中的各自不同的路径部分：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::&#123;cmp::Ordering, io&#125;;<br></code></pre></td></tr></table></figure><p>如果希望将一个路径下所有公有项引入作用域，可以指定路径后跟 <code>*</code>（glob 运算符）：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::collections::*;<br></code></pre></td></tr></table></figure><h4 id="as-关键字指定别名"><code>as</code> 关键字指定别名</h4><p>使用 <code>use</code> 将两个同名类型引入同一作用域可能会产生冲突。此时可以使用 <code>as</code> 关键字对引入的路径指定一个别名来区分彼此。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">use</span> std::fmt::<span class="hljs-built_in">Result</span>;<br><span class="hljs-keyword">use</span> std::io::<span class="hljs-built_in">Result</span> <span class="hljs-keyword">as</span> IoResult;<br><br><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">function1</span></span>() -&gt; <span class="hljs-built_in">Result</span> &#123;<br>    <span class="hljs-comment">// --snip--</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">function2</span></span>() -&gt; IoResult&lt;()&gt; &#123;<br>    <span class="hljs-comment">// --snip--</span><br>&#125;<br></code></pre></td></tr></table></figure><h4 id="pub-use-重导出"><code>pub use</code> 重导出</h4><p>当使用 <code>use</code> 关键字将名称导入作用域时，在新作用域中可用的名称是私有的。 如果要让调用新作用域模块的代码也能调用 <code>use</code> 引入的项，可以使用 <code>pub use</code> 来重导出项。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">mod</span> front_of_house &#123;<br>    <span class="hljs-keyword">pub</span> <span class="hljs-keyword">mod</span> hosting &#123;<br>        <span class="hljs-keyword">pub</span> <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">add_to_waitlist</span></span>() &#123;&#125;<br>    &#125;<br>&#125;<br><br><span class="hljs-keyword">pub</span> <span class="hljs-keyword">use</span> crate::front_of_house::hosting;<br><br><span class="hljs-keyword">pub</span> <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">eat_at_restaurant</span></span>() &#123;<br>    hosting::add_to_waitlist();<br>    hosting::add_to_waitlist();<br>    hosting::add_to_waitlist();<br>&#125;<br></code></pre></td></tr></table></figure></blockquote>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-简介&quot;&gt;1. 简介&lt;/h2&gt;
&lt;p&gt;Rust 的模块系统用来管理代码的组织，包括哪些内容可以被公开，哪些内容作为私有部分，以及程序每个作用域中的名字等。模块系统包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;包（Packages）：Cargo 的一个功能，允许构建、测试和分</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust方法和关联函数</title>
    <link href="https://blueheart0621.github.io/2021/04/21/Rust/Rust%E6%96%B9%E6%B3%95%E5%92%8C%E5%85%B3%E8%81%94%E5%87%BD%E6%95%B0/"/>
    <id>https://blueheart0621.github.io/2021/04/21/Rust/Rust%E6%96%B9%E6%B3%95%E5%92%8C%E5%85%B3%E8%81%94%E5%87%BD%E6%95%B0/</id>
    <published>2021-04-21T11:19:52.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-方法">1. 方法</h2><h3 id="1-1-简介">1.1 简介</h3><ul><li>方法与函数类似：它们使用 fn 关键字和名称声明，可以拥有参数和返回值，同时包含在某处调用该方法时会执行的代码。</li><li>不过方法与函数是不同的，方法是在结构体（或者枚举、trait 对象）的上下文中被定义的。</li><li>为了使方法位于指定结构体的上下文中，需要将方法放置在该结构体的 <code>impl</code> 块中。</li><li>方法的第一个参数总是 <code>self</code>，它代表调用该方法的结构体实例。</li></ul><h3 id="1-2-格式">1.2 格式</h3><p>定义一个结构体的方法示例如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Rectangle</span></span> &#123;<br>    width: <span class="hljs-built_in">u32</span>,<br>    height: <span class="hljs-built_in">u32</span>,<br>&#125;<br><span class="hljs-comment">// 定义 Rectangle 的方法</span><br><span class="hljs-keyword">impl</span> Rectangle &#123;<br>    <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">area</span></span>(&amp;<span class="hljs-keyword">self</span>) -&gt; <span class="hljs-built_in">u32</span> &#123;<br>        <span class="hljs-keyword">self</span>.width * <span class="hljs-keyword">self</span>.height<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="2-关联函数">2. 关联函数</h2><h3 id="2-1-简介">2.1 简介</h3><ul><li>关联函数与结构体相关联，同样在 <code>impl</code> 块中定义。它和方法的不同在于它不以 <code>self</code> 作为参数，因此它不是一个结构体的实例。</li><li>关联函数经常被用作返回一个结构体新实例的构造函数。</li><li>使用结构体名和 <code>::</code> 语法来调用关联函数。<code>::</code> 语法用于关联函数和模块创建的命名空间。</li></ul><h3 id="2-2-格式">2.2 格式</h3><p>定义一个结构体的关联函数示例如下:</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Rectangle</span></span> &#123;<br>    width: <span class="hljs-built_in">u32</span>,<br>    height: <span class="hljs-built_in">u32</span>,<br>&#125;<br><span class="hljs-comment">// 定义 Rectangle 的方法</span><br><span class="hljs-keyword">impl</span> Rectangle &#123;<br>    <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">area</span></span>(&amp;<span class="hljs-keyword">self</span>) -&gt; <span class="hljs-built_in">u32</span> &#123;<br>        <span class="hljs-keyword">self</span>.width * <span class="hljs-keyword">self</span>.height<br>    &#125;<br>    <span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">square</span></span>(size: <span class="hljs-built_in">u32</span>) -&gt; Rectangle &#123;<br>        Rectangle &#123; width: size, height: size &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-方法&quot;&gt;1. 方法&lt;/h2&gt;
&lt;h3 id=&quot;1-1-简介&quot;&gt;1.1 简介&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;方法与函数类似：它们使用 fn 关键字和名称声明，可以拥有参数和返回值，同时包含在某处调用该方法时会执行的代码。&lt;/li&gt;
&lt;li&gt;不过方法与函数是不同的，</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust所有权</title>
    <link href="https://blueheart0621.github.io/2021/04/19/Rust/Rust%E6%89%80%E6%9C%89%E6%9D%83/Rust%E6%89%80%E6%9C%89%E6%9D%83/"/>
    <id>https://blueheart0621.github.io/2021/04/19/Rust/Rust%E6%89%80%E6%9C%89%E6%9D%83/Rust%E6%89%80%E6%9C%89%E6%9D%83/</id>
    <published>2021-04-19T07:14:39.000Z</published>
    <updated>2021-04-28T03:45:37.099Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-简介">1. 简介</h2><p>所有运行的程序都必须管理其使用计算机内存的方式。一些语言中具有垃圾回收机制，在程序运行时不断地寻找不再使用的内存；在另一些语言中，程序员必须亲自分配和释放内存。Rust 则选择了第三种方式：通过所有权系统管理内存，编译器在编译时会根据一系列的规则进行检查。在运行时，所有权系统的任何功能都不会减慢程序。</p><h2 id="2-所有权规则">2. 所有权规则</h2><ul><li>Rust 中每一个值都有一个被称为「所有者」的变量。</li><li>值在任一时刻有且只有一个所有者。</li><li>当所有者离开作用域，这个值将被丢弃（内存回收）。</li></ul><h3 id="2-1-作用域">2.1 作用域</h3><p>变量的作用域是其在程序中有效的范围，一个变量作用域从声明的地方开始一直持续到最后一次使用为止，且其作用域被限制在变量声明所处的最内层 <code>&#123;&#125;</code> 代码块中，即最大不能超出其所处的最内层 <code>&#123;&#125;</code> 区域。</p><h3 id="2-2-垃圾回收">2.2 垃圾回收</h3><ul><li>在有垃圾回收（garbage collector，GC）的语言中， GC 记录并清除不再使用的内存，而我们并不需要关心它。</li><li>在没有 GC 的语言中，需要手动识别出不再使用的内存并调用代码显式释放，跟请求内存的时候一样。</li></ul><p>Rust 采取了一个不同的策略：内存在拥有它的变量离开作用域后就被自动释放。</p><h2 id="3-变量与数据交互方式">3. 变量与数据交互方式</h2><p>以 Rust 标准库中的 <code>String</code> 数据类型为例。一个 <code>String</code> 类型的变量实际由三部分组成：</p><ul><li>一个指向存放字符串内容内存的指针字段</li><li>一个长度字段</li><li>一个容量字段</li></ul><p>以上三部分都是存储在栈上，而由指针指向的字符串是存储在堆上的。</p><p><img src="/img/Rust/Rust%E6%89%80%E6%9C%89%E6%9D%83/String.svg" alt="String" /></p><h3 id="3-1-移动">3.1 移动</h3><ul><li>对于在栈上的变量，本质上并没有移动的概念。</li><li>对于在堆上的变量，比如 <code>String</code>，当将一个 <code>String</code> 变量赋值给另一个 <code>String</code> 变量时，拷贝的只是存储在栈上的内容：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> s1 = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;hello&quot;</span>);<br><span class="hljs-keyword">let</span> s2 = s1;<br></code></pre></td></tr></table></figure><p><img src="/img/Rust/Rust%E6%89%80%E6%9C%89%E6%9D%83/StringAsign.svg" alt="StringAsign" /></p><p>因此，两个数据指针同时指向同一个堆内存位置，即同一个堆内存地址有两个所有者，这样会导致在垃圾回收时出现「二次释放」的错误。即当 <code>s1</code> 和 <code>s2</code> 离开作用域是都将尝试释放同一块堆内存。</p><ul><li>为了确保内存安全，与其尝试拷贝被分配的内存，Rust 则认为 <code>s1</code> 不再有效，因此 Rust 不需要在 <code>s1</code> 离开作用域后清理任何东西。</li><li>当将 <code>s1</code> 赋值给 <code>s2</code> 后，<code>s1</code> 就失效了，往后对它的访问都会引发错误。</li><li>这样看起来将 <code>s1</code> 赋值给 <code>s2</code> 不是「拷贝」，而是「移动」。</li></ul><p><img src="/img/Rust/Rust%E6%89%80%E6%9C%89%E6%9D%83/StringMove.svg" alt="StringMove" /></p><p>【注】「将值传递给函数」以及「将值从函数返回」在语义上与给变量赋值相似。</p><h3 id="3-2-克隆">3.2 克隆</h3><ul><li>对于栈上的变量，将一个变量赋值给另一个变量即为克隆。</li><li>对于堆上的变量，将一个变量赋值给另一个变量实为移动，如果确实需要赋值 <code>s1</code> 中堆上的数据，而不仅仅是栈上的数据，可以使用 <code>clone</code> 函数来实现克隆。克隆后的两个 <code>String</code> 变量指向的堆内存是不相同的，因此都是有效的：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> s1 = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;hello&quot;</span>);<br><span class="hljs-keyword">let</span> s2 = s1.clone();<br><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;x = &#123;&#125;, y = &#123;&#125;&quot;</span>, x, y);<br></code></pre></td></tr></table></figure><p><img src="/img/Rust/Rust%E6%89%80%E6%9C%89%E6%9D%83/StringClone.svg" alt="StringClone" /></p><p>【注】当出现 <code>clone</code> 调用时，一些特定的代码被执行而且这些代码可能相当消耗资源。</p><h2 id="4-引用">4. 引用</h2><p>如果我们想将一个 <code>String</code> 变量传给调用函数，并在调用函数后仍然能够使用该 <code>String</code>：</p><ul><li>一种方式是将该 <code>String</code> 作为函数返回值的一部分，但这过于繁琐。</li><li>另一种方式就是使用不获取值的所有权的「引用」。</li></ul><p>在 Rust 中，使用 <code>&amp;</code> 来获取一个变量的引用。变量的引用允许使用值但不获得其所有权。</p><p><img src="/img/Rust/Rust%E6%89%80%E6%9C%89%E6%9D%83/Reference.svg" alt="Reference" /></p><ul><li>引用可以看作是一种特殊的变量，其有效作用域和普通变量一样。</li><li>当引用离开作用域后并不丢弃它指向的数据，因为它没有指向的数据的所有权。</li><li>正如变量默认是不可变的，引用也一样，（默认）不允许修改引用的值。</li><li>若需要创建可变引用，首先需要将原 <code>String</code> 变量设为 <code>mut</code>，然后使用 <code>&amp;mut</code> 创建可变引用。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> s = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;hello&quot;</span>);<br><span class="hljs-keyword">let</span> r = &amp;<span class="hljs-keyword">mut</span> s;<br></code></pre></td></tr></table></figure><ul><li>可变引用有一个很大的限制：在特定作用域中的特定数据只能有一个可变引用，而且也不能在拥有不可变引用的同时拥有可变引用。</li><li>一个引用的作用域从声明的地方开始一直持续到最后一次使用为止，因此在最后一次使用不可变引用后是可以声明可变引用的（因为它们的作用域没有重叠）。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> s = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;hello&quot;</span>);<br><br><span class="hljs-keyword">let</span> r1 = &amp;s; <span class="hljs-comment">// 没问题</span><br><span class="hljs-keyword">let</span> r2 = &amp;s; <span class="hljs-comment">// 没问题</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125; and &#123;&#125;&quot;</span>, r1, r2);<br><span class="hljs-comment">// 此位置之后 r1 和 r2 不再使用</span><br><br><span class="hljs-keyword">let</span> r3 = &amp;<span class="hljs-keyword">mut</span> s; <span class="hljs-comment">// 没问题</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, r3);<br></code></pre></td></tr></table></figure><ul><li>编译器会确保指向 String 的引用持续有效。</li></ul><p>【注】在任意给定时间，要么只能有一个可变引用，要么只能有多个不可变引用；而且在作用域内引用必须总是有效的。</p><p>在 Rust 中，将获取「引用」作为函数参数称为「借用」。</p><h2 id="5-Slice">5. Slice</h2><p>Slice 是一种特殊的引用，它允许你引用集合中一段连续的元素序列，而不用引用整个集合。可以使用一个由中括号中的 <code>[starting_index..ending_index]</code> 指定的 range 创建一个 Slice：</p><ul><li><code>starting_index</code> 是 Slice 的第一个位置，<code>ending_index</code> 则是 Slice 最后一个位置的后一个值。</li><li>在其内部，Slice 的数据结构存储了 Slice 的开始位置和长度，长度对应于 <code>ending_index</code> 减去 <code>starting_index</code> 的值。</li><li>如果 Slice 包含第一个索引（0），可以不写两个点号之前的值。</li><li>如果 Slice 包含最后一个索引，可以不写两个点号之后的值。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> s = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;hello world&quot;</span>);<br><br><span class="hljs-keyword">let</span> hello = &amp;s[<span class="hljs-number">0</span>..<span class="hljs-number">5</span>];<br><span class="hljs-keyword">let</span> world = &amp;s[<span class="hljs-number">6</span>..<span class="hljs-number">11</span>];<br></code></pre></td></tr></table></figure><p><img src="/img/Rust/Rust%E6%89%80%E6%9C%89%E6%9D%83/Slice.svg" alt="Slice" /></p><p>了解了 Slice，就可以正确地理解「字符串字面值」：字符串字面值本质就是 Slice。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> s = <span class="hljs-string">&quot;Hello, world!&quot;</span>;<br></code></pre></td></tr></table></figure><p>其中，<code>s</code> 的类型是 <code>&amp;str</code>，它是一个执行二进制程序特定位置的 Slice。这也就是为什么字符串字面值是不可变的，因为 <code>&amp;str</code> 是一个不可变引用。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 读取字符串第一个单词</span><br><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">first_word</span></span>(s: &amp;<span class="hljs-built_in">str</span>) -&gt; &amp;<span class="hljs-built_in">str</span> &#123;<br>    <span class="hljs-keyword">let</span> bytes = s.as_bytes();<br><br>    <span class="hljs-keyword">for</span> (i, &amp;item) <span class="hljs-keyword">in</span> bytes.iter().enumerate() &#123;<br>        <span class="hljs-keyword">if</span> item == <span class="hljs-string">b&#x27; &#x27;</span> &#123;<br>            <span class="hljs-keyword">return</span> &amp;s[<span class="hljs-number">0</span>..i];<br>        &#125;<br>    &#125;<br><br>    &amp;s[..]<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">main</span></span>() &#123;<br>    <span class="hljs-keyword">let</span> my_string = <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;hello world&quot;</span>);<br><br>    <span class="hljs-comment">// first_word 中传入 `String` 的 Slice</span><br>    <span class="hljs-keyword">let</span> word = first_word(&amp;my_string[..]);<br><br>    <span class="hljs-keyword">let</span> my_string_literal = <span class="hljs-string">&quot;hello world&quot;</span>;<br><br>    <span class="hljs-comment">// first_word 中传入字符串字面值的 Slice</span><br>    <span class="hljs-keyword">let</span> word = first_word(&amp;my_string_literal[..]);<br><br>    <span class="hljs-comment">// 因为字符串字面值就是字符串 Slice，</span><br>    <span class="hljs-comment">// 这样写也可以，即不使用 Slice 语法！</span><br>    <span class="hljs-keyword">let</span> word = first_word(my_string_literal);<br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-简介&quot;&gt;1. 简介&lt;/h2&gt;
&lt;p&gt;所有运行的程序都必须管理其使用计算机内存的方式。一些语言中具有垃圾回收机制，在程序运行时不断地寻找不再使用的内存；在另一些语言中，程序员必须亲自分配和释放内存。Rust 则选择了第三种方式：通过所有权系统管理内存，编译器在编</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust控制流</title>
    <link href="https://blueheart0621.github.io/2021/04/19/Rust/Rust%E6%8E%A7%E5%88%B6%E6%B5%81/"/>
    <id>https://blueheart0621.github.io/2021/04/19/Rust/Rust%E6%8E%A7%E5%88%B6%E6%B5%81/</id>
    <published>2021-04-19T06:11:25.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-条件结构">1. 条件结构</h2><p>Rust 中主要的条件结构为 <code>if</code> 表达式。</p><h3 id="1-1-if-表达式">1.1 <code>if</code> 表达式</h3><p><code>if</code> 表达式的基本语法格式如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">if</span> &lt;condition1&gt; &#123;<br>...<br>&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> &lt;condition2&gt; &#123;<br>...<br>&#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> &lt;conditionn&gt; &#123;<br>...<br>&#125; <span class="hljs-keyword">else</span> &#123;<br>...<br>&#125;<br></code></pre></td></tr></table></figure><p>其中，<code>else if</code> 和 <code>else</code> 分支都是可选的。表达式中的条件 <code>conditionX</code> 必须是 <code>bool</code> 类型的。</p><ul><li>由于是表达式，因此可以将 <code>if</code> 表达式的值赋值给变量。</li><li>由于 Rust 是静态编译语言，要求在编译时就确定所有变量的类型，因此 <code>if</code> 表达式的每个分支可能的返回值都必须相同。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> condition = <span class="hljs-literal">true</span>;<br><span class="hljs-keyword">let</span> branch = <span class="hljs-keyword">if</span> condition &#123;<br><span class="hljs-number">1</span><br>&#125; <span class="hljs-keyword">else</span> &#123;<br><span class="hljs-number">2</span><br>&#125;<br></code></pre></td></tr></table></figure><h2 id="2-循环结构">2. 循环结构</h2><p>Rust 中提供的循环结构有 <code>loop</code>、<code>while</code> 和 <code>for</code>。Rust 提供 <code>break</code> 表达式用于退出循环，并可选地返回一个返回值。</p><h3 id="2-1-loop">2.1 <code>loop</code></h3><p><code>loop</code> 结构表示一直执行循环体，直到遇到 <code>break</code> 表达式。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> counter = <span class="hljs-number">0</span>;<br><br><span class="hljs-keyword">let</span> result = <span class="hljs-keyword">loop</span> &#123;<br>counter += <span class="hljs-number">1</span>;<br><br><span class="hljs-keyword">if</span> counter == <span class="hljs-number">10</span> &#123;<br><span class="hljs-keyword">break</span> counter * <span class="hljs-number">2</span>;<br>&#125;<br>&#125;;<br><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The result is &#123;&#125;&quot;</span>, result);<br></code></pre></td></tr></table></figure><h3 id="2-2-while">2.2 <code>while</code></h3><p><code>while</code> 结构中，<code>while</code> 关键字后接一个条件表达式用于判断循环是否继续，从而实现条件循环。当然，<code>while</code> 结构可以通过组合 <code>loop</code>、<code>if</code>、<code>else</code> 和 <code>break</code> 来实现。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> number = <span class="hljs-number">3</span>;<br><br><span class="hljs-keyword">while</span> number != <span class="hljs-number">0</span> &#123;<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;&#123;&#125;&quot;</span>, number);<br><br>number = number - <span class="hljs-number">1</span>;<br>&#125;<br><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;LIFTOFF!!!&quot;</span>);<br></code></pre></td></tr></table></figure><h3 id="2-3-for">2.3 <code>for</code></h3><p><code>for</code> 结构可以用来遍历集合中的元素和范围空间。虽然可以通过 <code>while</code> 和索引值判断来实现 <code>for</code> 模式，但这个过程很容易出错，也使得程序更慢，因为编译器增加了运行时代码来对每次循环的每个元素进行条件检查。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> a = [<span class="hljs-number">10</span>, <span class="hljs-number">20</span>, <span class="hljs-number">30</span>, <span class="hljs-number">40</span>, <span class="hljs-number">50</span>];<br><br><span class="hljs-comment">// 遍历元素集合</span><br><span class="hljs-keyword">for</span> element <span class="hljs-keyword">in</span> a.iter() &#123;<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The value is: &#123;&#125;&quot;</span>, element);<br>&#125;<br><br><span class="hljs-comment">// 遍历范围区间</span><br><span class="hljs-comment">// 使用了标准库提供的 Range 类型</span><br><span class="hljs-keyword">for</span> number <span class="hljs-keyword">in</span> (<span class="hljs-number">1</span>..<span class="hljs-number">4</span>).rev() &#123;<span class="hljs-comment">// rev 方法用于反转 range</span><br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The value is: &#123;&#125;&quot;</span>, number);<br>&#125;<br></code></pre></td></tr></table></figure><p>【注】 <code>for</code> 循环的安全性和简洁性使得它成为 Rust 中使用最多的循环结构。</p><h2 id="3-匹配结构">3. 匹配结构</h2><p>Rust 有一个叫做 <code>match</code> 的极为强大的控制流运算符，它允许我们将一个值与一系列的模式相比较，并根据相匹配的模式执行相应代码。模式可由字面值、变量、通配符和许多其他内容构成。一个 <code>match</code> 匹配示例如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">Coin</span></span> &#123;<br>    Penny,<br>    Nickel,<br>    Dime,<br>    Quarter,<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">value_in_cents</span></span>(coin: Coin) -&gt; <span class="hljs-built_in">u8</span> &#123;<br>    <span class="hljs-keyword">match</span> coin &#123;<br>        Coin::Penny =&gt; <span class="hljs-number">1</span>,<br>        Coin::Nickel =&gt; <span class="hljs-number">5</span>,<br>        Coin::Dime =&gt; <span class="hljs-number">10</span>,<br>        Coin::Quarter =&gt; <span class="hljs-number">25</span>,<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>match</code> 的一个分支有两个部分：一个模式和一些代码，二者使用 <code>=&gt;</code> 分隔。每个分支之间使用 <code>,</code> 分隔。</li><li>当 <code>match</code> 表达式执行时，它将结果值按顺序与每一个分支的模式相比较。如果模式匹配了这个值，这个模式相关联的代码将被执行。如果模式并不匹配这个值，将继续执行下一个分支。</li><li><code>match</code> 分支可以绑定匹配模式的部分值，即从枚举成员中提取值。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">UsState</span></span> &#123;<br>    Alabama,<br>    Alaska,<br>    <span class="hljs-comment">// --snip--</span><br>&#125;<br><br><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">Coin</span></span> &#123;<br>    Penny,<br>    Nickel,<br>    Dime,<br>    Quarter(UsState),<br>&#125;<br><br><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">value_in_cents</span></span>(coin: Coin) -&gt; <span class="hljs-built_in">u8</span> &#123;<br>    <span class="hljs-keyword">match</span> coin &#123;<br>        Coin::Penny =&gt; <span class="hljs-number">1</span>,<br>        Coin::Nickel =&gt; <span class="hljs-number">5</span>,<br>        Coin::Dime =&gt; <span class="hljs-number">10</span>,<br>        Coin::Quarter(state) =&gt; &#123;<br>            <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;State quarter from &#123;:?&#125;!&quot;</span>, state);<br>            <span class="hljs-number">25</span><br>        &#125;,<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>match</code> 必须覆盖所有可能的模式匹配分支，即所谓「穷尽性检查」。不过可以使用 <code>_</code> 通配符来实现列举其余未列举出的分支。由于 <code>_</code> 通配符会匹配所有的值，所以需要将其放在最后一个分支。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> some_u8_value = <span class="hljs-number">0u8</span>;<br><span class="hljs-keyword">match</span> some_u8_value &#123;<br>    <span class="hljs-number">1</span> =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;one&quot;</span>),<br>    <span class="hljs-number">3</span> =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;three&quot;</span>),<br>    <span class="hljs-number">5</span> =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;five&quot;</span>),<br>    <span class="hljs-number">7</span> =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;seven&quot;</span>),<br>    _ =&gt; (),<br>&#125;<br></code></pre></td></tr></table></figure><blockquote><h4 id="if-let-简单控制流"><code>if let</code> 简单控制流</h4><p>可以看到，<code>match</code> 对于处理只有很少甚至一个分支的情况就比较啰嗦了。另一个简单的控制流 <code>if let</code> 语法让我们以一种不那么冗长的方式结合 <code>if</code> 和 <code>let</code>，来处理只匹配一个模式的值而忽略其他模式的情况。</p><ul><li>匹配一个模式举例：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// match 实现</span><br><span class="hljs-keyword">let</span> some_u8_value = <span class="hljs-literal">Some</span>(<span class="hljs-number">0u8</span>);<br><span class="hljs-keyword">match</span> some_u8_value &#123;<br>    <span class="hljs-literal">Some</span>(<span class="hljs-number">3</span>) =&gt; <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;three&quot;</span>),<br>    _ =&gt; (),<br>&#125;<br><span class="hljs-comment">// if let 实现单模式分支匹配</span><br><span class="hljs-keyword">if</span> <span class="hljs-keyword">let</span> <span class="hljs-literal">Some</span>(<span class="hljs-number">3</span>) = some_u8_value &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;three&quot;</span>);<br>&#125;<br><span class="hljs-comment">// if let 实现 match 匹配</span><br><span class="hljs-keyword">if</span> <span class="hljs-keyword">let</span> <span class="hljs-literal">Some</span>(<span class="hljs-number">3</span>) = some_u8_value &#123;<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;three&quot;</span>);<br>&#125; <span class="hljs-keyword">else</span> &#123;<br>    ()<br>&#125;<br></code></pre></td></tr></table></figure><ul><li><code>if let</code> 获取通过等号分隔的一个模式和一个表达式，它的工作方式与 match 相同。</li><li><code>if let</code> 可以使代码更简洁，但会丢失 <code>match</code> 强制要求的穷尽性检查。</li></ul></blockquote>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-条件结构&quot;&gt;1. 条件结构&lt;/h2&gt;
&lt;p&gt;Rust 中主要的条件结构为 &lt;code&gt;if&lt;/code&gt; 表达式。&lt;/p&gt;
&lt;h3 id=&quot;1-1-if-表达式&quot;&gt;1.1 &lt;code&gt;if&lt;/code&gt; 表达式&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;if&lt;/code&gt; </summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>代码风格规范</title>
    <link href="https://blueheart0621.github.io/2021/04/19/Programming/%E4%BB%A3%E7%A0%81%E9%A3%8E%E6%A0%BC%E8%A7%84%E8%8C%83/"/>
    <id>https://blueheart0621.github.io/2021/04/19/Programming/%E4%BB%A3%E7%A0%81%E9%A3%8E%E6%A0%BC%E8%A7%84%E8%8C%83/</id>
    <published>2021-04-19T05:29:32.000Z</published>
    <updated>2021-04-28T03:45:37.059Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-简介">1. 简介</h2><p>各种编程语言层出不穷，各种语言的代码风格规范也不尽相同。主流的代码风格规范有：camel case、snake case、kebab case。</p><h2 id="2-代码风格">2. 代码风格</h2><h3 id="2-1-camel-case（驼峰式）">2.1 camel case（驼峰式）</h3><p>camel case 式命名的名称中，单词之间直接相连，除了第一个单词外后面单词首字母均大写。</p><ul><li>如果第一个单词首字母大写，则称为 <code>CamelCase</code>（大驼峰式）。</li><li>如果第一个单词首字母小写，则称为 <code>camelCase</code>（小驼峰式）。</li></ul><h3 id="2-2-snake-case（蛇式）">2.2 snake case（蛇式）</h3><p>snake case 式命名的名称中，使用 <code>_</code> 来分隔相邻单词。</p><ul><li>如果所有单词都小写，称为 <code>lower_snake_case</code>（小蛇式）。</li><li>如果所有单词都大写，称为 <code>UPPER_SNAKE_CASE</code>（大蛇式）。</li></ul><h3 id="2-3-kebab-case（烤肉串式）">2.3 kebab case（烤肉串式）</h3><p>kebab case 式命名的名称中，使用 <code>-</code> 来分隔相邻单词，所有单词都小写。</p><h2 id="附录">附录</h2><p>参考资料来源于：</p><ul><li><a href="https://en.wikipedia.org/wiki/Letter_case#Special_case_styles">Letter case</a></li><li><a href="https://en.wikipedia.org/wiki/Camel_case">Camel case</a></li><li><a href="https://en.wikipedia.org/wiki/Snake_case">Snake case</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-简介&quot;&gt;1. 简介&lt;/h2&gt;
&lt;p&gt;各种编程语言层出不穷，各种语言的代码风格规范也不尽相同。主流的代码风格规范有：camel case、snake case、kebab case。&lt;/p&gt;
&lt;h2 id=&quot;2-代码风格&quot;&gt;2. 代码风格&lt;/h2&gt;
&lt;h3 id</summary>
      
    
    
    
    <category term="Programming" scheme="https://blueheart0621.github.io/categories/Programming/"/>
    
    
    <category term="Programming" scheme="https://blueheart0621.github.io/tags/Programming/"/>
    
  </entry>
  
  <entry>
    <title>Rust函数</title>
    <link href="https://blueheart0621.github.io/2021/04/19/Rust/Rust%E5%87%BD%E6%95%B0/"/>
    <id>https://blueheart0621.github.io/2021/04/19/Rust/Rust%E5%87%BD%E6%95%B0/</id>
    <published>2021-04-19T04:42:39.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-格式">1. 格式</h2><p>Rust 代码中函数定义的基本语法格式如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-function"><span class="hljs-keyword">fn</span> </span>&lt;function&gt;(&lt;parameters&gt;) &#123;<br><span class="hljs-comment">// 函数体</span><br>...<br>&#125;<br></code></pre></td></tr></table></figure><p>其中，<code>fn</code> 为函数声明关键字，<code>&lt;function&gt;</code> 为函数名，<code>&lt;parameters&gt;</code> 为参数列表，<code>&#123;&#125;</code> 用于指定函数体的开始和结束。和大多数编程语言一样，Rust 中 <code>main</code> 函数也是程序的入口点。</p><h2 id="2-参数">2. 参数</h2><p>Rust 函数可以定义输入参数列表 <code>&lt;parameters&gt;</code>。在 <code>&lt;parameters&gt;</code> 中，必须声明每个参数的类型，多个参数用 <code>,</code> 分隔。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">example</span></span>(x: <span class="hljs-built_in">i32</span>, y: <span class="hljs-built_in">i32</span>) &#123;<br><span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The value of x is: &#123;&#125;&quot;</span>, x);<br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The value of y is: &#123;&#125;&quot;</span>, y);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="3-函数体">3. 函数体</h2><p>函数体由一系列的语句和一个可选的结尾表达式构成。</p><ul><li>语句（Statements）是执行一些操作但不返回值的指令。</li><li>表达式（Expressions）计算并产生一个值。</li></ul><p>在 Rust 中，<strong>语句的结尾有分号，表达式的结尾没有分号</strong>。如果在表达式的结尾加上分号，它就变成了语句，而语句不会返回值。</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">example</span></span>() &#123;<br>    <span class="hljs-keyword">let</span> x = <span class="hljs-number">5</span>;<br><br>    <span class="hljs-keyword">let</span> y = &#123;<br>        <span class="hljs-keyword">let</span> x = <span class="hljs-number">3</span>;<span class="hljs-comment">// 语句，不会返回值</span><br>        x + <span class="hljs-number">1</span><span class="hljs-comment">// 表达式，会返回值</span><br>    &#125;;<br><span class="hljs-comment">// 因此 y 的值为 4</span><br>    <span class="hljs-built_in">println!</span>(<span class="hljs-string">&quot;The value of y is: &#123;&#125;&quot;</span>, y);<br>&#125;<br></code></pre></td></tr></table></figure><h2 id="4-返回值">4. 返回值</h2><p>同其他语言一样，Rust 函数可以向调用它的代码返回值。Rust 中并不对返回值命名，但需要在箭头 <code>-&gt;</code> 后声明它的类型。</p><ul><li>在 Rust 函数中，函数的返回值等同于函数最后一个表达式的值。</li><li>使用 <code>return</code> 关键字和指定值，可以在函数中提前返回。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">example</span></span>() -&gt; <span class="hljs-built_in">i32</span> &#123;<br>    <span class="hljs-number">5</span><br>&#125;<br></code></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-格式&quot;&gt;1. 格式&lt;/h2&gt;
&lt;p&gt;Rust 代码中函数定义的基本语法格式如下：&lt;/p&gt;
&lt;figure class=&quot;highlight rust&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust基本数据类型</title>
    <link href="https://blueheart0621.github.io/2021/04/19/Rust/Rust%E5%9F%BA%E6%9C%AC%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/"/>
    <id>https://blueheart0621.github.io/2021/04/19/Rust/Rust%E5%9F%BA%E6%9C%AC%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/</id>
    <published>2021-04-19T03:06:34.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-简介">1. 简介</h2><p>在 Rust 中，每个值都属于某一个数据类型，用来告诉 Rust 它被指定为何种数据，以便明确数据处理方式。Rust 基本数据类型主要有两类子集：标量（scalar）和复合（compound）。</p><ul><li>此文所讲的基本数据类型都是 Rust 原生的数据类型，它们都是创建在「栈」上的数据结构。</li><li>Rust 标准库还提供了一些更复杂的数据类型，它们有些是创建在「堆」上的数据结构，比如下文提到的 <code>vector</code> 数据类型。</li></ul><p>【注】Rust 是静态类型语言，因此在编译时就必须知道所有变量的类型。通常，根据值及其使用方式，Rust 编译器可以推断出我们想要用的类型；当多种类型均有可能时，必须增加类型注解，否则编译会报错。</p><h2 id="2-标量类型">2. 标量类型</h2><p>标量（scalar）类型代表一个单独的值。Rust 有四种基本的标量类型：整型、浮点型、布尔类型和字符类型。</p><h3 id="2-1-整型">2.1 整型</h3><p>Rust 内建的整数类型如下表所示：</p><table><thead><tr><th style="text-align:left">长度</th><th style="text-align:left">有符号</th><th style="text-align:left">无符号</th></tr></thead><tbody><tr><td style="text-align:left">8-bit</td><td style="text-align:left"><code>i8</code></td><td style="text-align:left"><code>u8</code></td></tr><tr><td style="text-align:left">16-bit</td><td style="text-align:left"><code>i16</code></td><td style="text-align:left"><code>u16</code></td></tr><tr><td style="text-align:left">32-bit</td><td style="text-align:left"><code>i32</code></td><td style="text-align:left"><code>u32</code></td></tr><tr><td style="text-align:left">64-bit</td><td style="text-align:left"><code>i64</code></td><td style="text-align:left"><code>u64</code></td></tr><tr><td style="text-align:left">128-bit</td><td style="text-align:left"><code>i128</code></td><td style="text-align:left"><code>u128</code></td></tr><tr><td style="text-align:left">arch</td><td style="text-align:left"><code>isize</code></td><td style="text-align:left"><code>usize</code></td></tr></tbody></table><p>其中，arch 长度依赖于运行程序的计算机架构：64 位架构上为 64-bit，32 位架构上为 32-bit。</p><p>Rust 中书写数字字面值的形式如下表所示：</p><table><thead><tr><th style="text-align:left">数字字面值</th><th style="text-align:left">举例</th></tr></thead><tbody><tr><td style="text-align:left">Decimal（十进制）</td><td style="text-align:left"><code>98_222</code></td></tr><tr><td style="text-align:left">Hex（十六进制）</td><td style="text-align:left"><code>0xff</code></td></tr><tr><td style="text-align:left">Octal（八进制）</td><td style="text-align:left"><code>0o77</code></td></tr><tr><td style="text-align:left">Binary（二进制）</td><td style="text-align:left"><code>0b1111_0000</code></td></tr><tr><td style="text-align:left">Byte（单字节字符）</td><td style="text-align:left"><code>b'A'</code></td></tr></tbody></table><p>其中，Byte 的书写形式仅限于 <code>u8</code> 类型，R<code>_</code> 为分隔符以方便读数。</p><p>【注】Rust 的默认整型为 <code>i32</code>，它通常是最快的。</p><blockquote><p><strong>整型溢出</strong></p><ul><li>在 debug 模式下编译时，Rust 检查这类问题并使程序 panic，即表示程序因错误而退出。</li><li>在 release 模式下编译时，Rust 不检测溢出，而是会进行一种被称为二进制补码包装的操作（本质就是忽略溢出的位）。</li></ul></blockquote><h3 id="2-2-浮点型">2.2 浮点型</h3><p>Rust 有两个原生的浮点数类型：<code>f32</code> 和 <code>f64</code>，默认浮点数类型是 <code>f64</code>。浮点数采用 IEEE-754 标准表示，<code>法2</code> 是单精度浮点数，<code>f64</code> 是双精度浮点数。</p><p>【注】在现代 CPU 中，<code>f64</code> 与 <code>f32</code> 速度几乎是一样的。</p><h3 id="2-3-布尔类型">2.3 布尔类型</h3><p>Rust 中的布尔类型用 <code>bool</code> 声明。和其他语言类似，它两种取值：<code>true</code> 和 <code>false</code> 。</p><h3 id="2-4-字符类型">2.4 字符类型</h3><p>Rust 中的字符类型用 <code>char</code> 声明，它是 Rust 中最原生的字母类型。<code>char</code> 使用单引号指定，不同于字符串使用双引号指定。</p><ul><li>Rust 的 <code>char</code> 类型大小为四个字节，代表了一个 Unicode 标量值。</li><li>在 Rust 中，拼音字母、中文、日文、韩文等文字字符，甚至 emoji 和零长度的空白符都是有效的 char 值。</li></ul><h2 id="3-复合类型">3. 复合类型</h2><p>复合类型（compound）可以将多个值组合成一个类型，Rust 中原生的复合类型有：元组（tuple）、数组（array）、结构体（struct）。</p><h3 id="3-1-元组">3.1 元组</h3><p>元组是一个将多个其他类型的值组合进一个复合类型的主要方式。元组长度固定，一旦声明，其长度不能改变。元组的声明语法如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> tup: (<span class="hljs-built_in">i32</span>, <span class="hljs-built_in">f64</span>, <span class="hljs-built_in">u8</span>) = (<span class="hljs-number">500</span>, <span class="hljs-number">6.4</span>, <span class="hljs-number">1</span>);<br></code></pre></td></tr></table></figure><p><code>tup</code> 变量绑定到整个元组上，因为元组是一个单独的复合元素。为了从元组中获取单个值，可以使用模式匹配来「解构」元组值，或者直接使用 <code>.</code> 运算符按索引值（索引值从 0 开始）访问：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 解构</span><br><span class="hljs-keyword">let</span> (x, y, z) = tup;<br><span class="hljs-comment">// . 运算符</span><br><span class="hljs-keyword">let</span> x = tup.<span class="hljs-number">0</span>;<br><span class="hljs-keyword">let</span> y = tup.<span class="hljs-number">1</span>;<br><span class="hljs-keyword">let</span> z = tup.<span class="hljs-number">2</span>;<br></code></pre></td></tr></table></figure><h3 id="3-2-数组">3.2 数组</h3><p>另一个包含多个值的方式是数组，与元组不同，数组中的每个元素的类型必须相同。Rust 中的数组是固定长度的，一旦声明，其长度不能改变。数组的声明语法如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-comment">// 自动计算长度</span><br><span class="hljs-keyword">let</span> arr = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>];<br><span class="hljs-comment">// 指定类型和长度</span><br><span class="hljs-keyword">let</span> arr: [<span class="hljs-built_in">i32</span>; <span class="hljs-number">5</span>] = [<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>, <span class="hljs-number">4</span>, <span class="hljs-number">5</span>];<br><span class="hljs-comment">// 创建重复元素数组</span><br><span class="hljs-keyword">let</span> arr = [<span class="hljs-number">3</span>; <span class="hljs-number">5</span>];<span class="hljs-comment">// 创建包含 5 个元素值均为 3 的数组</span><br></code></pre></td></tr></table></figure><ul><li>数组是一整块分配在栈上的内存，可以使用索引来访问数组的元素：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> first = arr[<span class="hljs-number">0</span>];<br><span class="hljs-keyword">let</span> second = arr[<span class="hljs-number">1</span>];<br></code></pre></td></tr></table></figure><ul><li>如果访问数组时索引溢出，在编译时可以通过，但在运行时会报错 panic 而退出。</li></ul><p>【注】Rust 标准库中提供了 <code>vector</code> 集合类型，它可以实现数组长度的动态变化。</p><h3 id="3-3-结构体">3.3 结构体</h3><p>结构体和元组一样，结构体的每一部分可以是不同类型。但不同于元组，结构体需要命名各部分数据以便能清楚的表明其值的意义。由于有了这些名字，结构体比元组更灵活：不需要依赖顺序来指定或访问实例中的值。</p><ul><li>定义结构的基本语法类似如下：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">User</span></span> &#123;<br>    username: <span class="hljs-built_in">String</span>,<br>    email: <span class="hljs-built_in">String</span>,<br>    sign_in_count: <span class="hljs-built_in">u64</span>,<br>    active: <span class="hljs-built_in">bool</span>,<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>定义了结构体之后，可以通过为每个字段指定具体值来创建这个结构体的实例。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> user1 = User &#123;<br>    email: <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;someone@example.com&quot;</span>),<br>    username: <span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;someusername123&quot;</span>),<br>    active: <span class="hljs-literal">true</span>,<br>    sign_in_count: <span class="hljs-number">1</span>,<br>&#125;;<br></code></pre></td></tr></table></figure><ul><li>为了从结构体中获取某个特定的值，可以使用 <code>.</code> 运算符。</li><li>若想改变结构体实例中某个字段的值，则要求整个实例必须是可变的。Rust 并不允许只将某个字段标记为可变。</li></ul><blockquote><h4 id="元组结构体">元组结构体</h4><p>可以定义与元组类似的结构体，称为「元组结构体」。</p><ul><li>元组结构体有着结构体名称提供的含义，但没有具体的字段名，只有字段的类型。定义元组结构体举例如下：</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Color</span></span>(<span class="hljs-built_in">i32</span>, <span class="hljs-built_in">i32</span>, <span class="hljs-built_in">i32</span>);<br><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Point</span></span>(<span class="hljs-built_in">i32</span>, <span class="hljs-built_in">i32</span>, <span class="hljs-built_in">i32</span>);<br><br><span class="hljs-keyword">let</span> black = Color(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>);<br><span class="hljs-keyword">let</span> origin = Point(<span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>);<br></code></pre></td></tr></table></figure><ul><li>不同元组结构体的实例是不相同的，不能相互替代，即使结构体中的字段有着相同的类型。</li><li>在其他方面，元组结构体实例类似于元组：可以将其解构为单独的部分，也可以使用 <code>.</code> 后跟索引来访问单独的值。</li></ul><h4 id="自动引用和解引用">自动引用和解引用</h4><ul><li>在 C/C++ 语言中，有两个不同的运算符来调用字段：<code>.</code> 直接在对象上调用字段，而 <code>-&gt;</code> 在一个对象的指针上调用字段，这时需要先解引用（dereference）指针。</li><li>Rust 并没有一个与 <code>-&gt;</code> 等效的运算符；相反，Rust 有一个叫「自动引用和解引用」（automatic referencing and dereferencing）的功能。<ul><li>当 <code>object</code> 调用字段时，Rust 会自动为 <code>object</code> 添加 <code>&amp;</code>、<code>&amp;mut</code> 或 <code>*</code> 以便使 <code>object</code> 与字段签名匹配。即 <code>object.field</code> 和 <code>(&amp;object).field</code> 等价。</li></ul></li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">struct</span> <span class="hljs-title">Rectangle</span></span> &#123;<br>    width: <span class="hljs-built_in">u32</span>,<br>    height: <span class="hljs-built_in">u32</span>,<br>&#125;<br><span class="hljs-function"><span class="hljs-keyword">fn</span> <span class="hljs-title">main</span></span>() &#123;<br>    <span class="hljs-keyword">let</span> rect = Rectangle &#123; width: <span class="hljs-number">30</span>, height: <span class="hljs-number">50</span> &#125;;<br><br>    <span class="hljs-built_in">println!</span>(<br>        <span class="hljs-string">&quot;width: &#123;&#125;, height: &#123;&#125;&quot;</span>,<br>        rect.width,<br>        (&amp;rect).height<br>    );<br>&#125;<br></code></pre></td></tr></table></figure></blockquote><h3 id="3-4-枚举">3.4 枚举</h3><p>枚举（enumerations）允许通过列举可能的成员（variants）来定义一个类型。利用枚举列出可能的 IP 地址类型举例如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">IpAddrKind</span></span> &#123;<br>    V4,<br>    V6,<br>&#125;<br></code></pre></td></tr></table></figure><ul><li>访问枚举中的成员使用 <code>::</code> 语法：<code>IpAddrKind::V4</code> 和 <code>IpAddrKind::V6</code>。</li><li>每个枚举成员都是定义的枚举类型，即 <code>IpAddrKind::V4</code> 和 <code>IpAddrKind::V6</code> 都是 <code>IpAddrKind</code> 类型。</li><li>枚举还能将数据直接和枚举的每一个成员绑定，这样就不需要额外定义结构体来关联枚举成员和对应数据。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">IpAddr</span></span> &#123;<br>    V4(<span class="hljs-built_in">String</span>),<br>    V6(<span class="hljs-built_in">String</span>),<br>&#125;<br><br><span class="hljs-keyword">let</span> home = IpAddr::V4(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;127.0.0.1&quot;</span>));<br><span class="hljs-keyword">let</span> loopback = IpAddr::V6(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;::1&quot;</span>));<br></code></pre></td></tr></table></figure><ul><li>枚举的每一个成员可以处理不同类型和数量的数据。枚举成员甚至可以包含另一个枚举。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">IpAddr</span></span> &#123;<br>    V4(<span class="hljs-built_in">u8</span>, <span class="hljs-built_in">u8</span>, <span class="hljs-built_in">u8</span>, <span class="hljs-built_in">u8</span>),<br>    V6(<span class="hljs-built_in">String</span>),<br>&#125;<br><br><span class="hljs-keyword">let</span> home = IpAddr::V4(<span class="hljs-number">127</span>, <span class="hljs-number">0</span>, <span class="hljs-number">0</span>, <span class="hljs-number">1</span>);<br><span class="hljs-keyword">let</span> loopback = IpAddr::V6(<span class="hljs-built_in">String</span>::from(<span class="hljs-string">&quot;::1&quot;</span>));<br></code></pre></td></tr></table></figure><blockquote><h4 id="Rust-标准库中常用枚举">Rust 标准库中常用枚举</h4><ol><li>IpAddr 枚举：用来存储和编码 IP 地址。</li></ol><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">IpAddr</span></span> &#123;<br>    V4(Ipv4Addr),<br>    V6(Ipv6Addr),<br>&#125;<br></code></pre></td></tr></table></figure><ol start="2"><li>Option 枚举：用来表示一个值要么有值要么没值。</li></ol><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-class"><span class="hljs-keyword">enum</span> <span class="hljs-title">Option</span></span>&lt;T&gt; &#123;<br>    <span class="hljs-literal">Some</span>(T),<br>    <span class="hljs-literal">None</span>,<br>&#125;<br></code></pre></td></tr></table></figure></blockquote>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-简介&quot;&gt;1. 简介&lt;/h2&gt;
&lt;p&gt;在 Rust 中，每个值都属于某一个数据类型，用来告诉 Rust 它被指定为何种数据，以便明确数据处理方式。Rust 基本数据类型主要有两类子集：标量（scalar）和复合（compound）。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;此</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust常量与变量</title>
    <link href="https://blueheart0621.github.io/2021/04/19/Rust/Rust%E5%B8%B8%E9%87%8F%E4%B8%8E%E5%8F%98%E9%87%8F/"/>
    <id>https://blueheart0621.github.io/2021/04/19/Rust/Rust%E5%B8%B8%E9%87%8F%E4%B8%8E%E5%8F%98%E9%87%8F/</id>
    <published>2021-04-19T02:45:55.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-常量">1. 常量</h2><p>声明常量使用的关键字是 <code>const</code>。</p><ul><li>在声明常量时，必须注明值的类型。</li><li>常量可以在任何作用域中声明，包括全局作用域。常量在它的作用域中，整个程序生命周期中都有效。</li><li>常量只能被设置为常量表达式，而不能是函数调用的结果，或其他任何只能在运行时计算出的值。</li><li>将遍布于应用程序中的硬编码值声明为常量，能帮助后来的代码维护人员了解值的意图。如果将来需要修改硬编码值，也只需修改汇聚于一处的硬编码值。</li></ul><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">const</span> MAX_POINTS: <span class="hljs-built_in">u32</span> = <span class="hljs-number">100_000</span>;<br></code></pre></td></tr></table></figure><h2 id="2-变量">2. 变量</h2><p>Rust 中的变量是默认不可变的（immutable），这是推动我们充分利用 Rust 提供的安全性和简单并发性来编写代码的众多方式之一。不过我们仍然可以使用可变变量。声明变量使用的关键字是 <code>let</code> 。</p><h3 id="2-1-不变变量">2.1 不变变量</h3><p>当变量不可变时，一旦值被绑定一个名称上，就不能改变这个值。定义变量的基本语法如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> var = &lt;value&gt;;<br></code></pre></td></tr></table></figure><p>这种方式定义的变量默认是不可变的。</p><h3 id="2-2-可变变量">2.2 可变变量</h3><p>定义的变量只是默认不可变，我们可以在变量名加上 <code>mut</code> 来使其可变：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> <span class="hljs-keyword">mut</span> var = &lt;value&gt;;<br></code></pre></td></tr></table></figure><p>这种方式定义的变量就是可变的。</p><h3 id="2-3-隐藏变量">2.3 隐藏变量</h3><p>Rust 中可以重复定义相同的变量名，这样新变量会隐藏之前的变量。举例如下：</p><figure class="highlight rust"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs rust"><span class="hljs-keyword">let</span> var = &lt;value&gt;;<br><span class="hljs-keyword">let</span> var = var + <span class="hljs-number">1</span>;<br><span class="hljs-keyword">let</span> var = var * <span class="hljs-number">2</span>;<br></code></pre></td></tr></table></figure><p>如果第二、三行前不加 <code>let</code>，编译是会报错的，因为第一行定义的 <code>var</code> 变量是不可变的。而加上 <code>let</code> 表示隐藏了前面定义的 <code>var</code> 变量。</p><p>【注】隐藏和可变是有区别的：</p><ul><li>隐藏定义的变量仍然是不可变的，可变变量始终的可变的。</li><li>隐藏定义变量实际是创建了一个新变量，因此可以改变值的类型，只是复用同一个变量名；而可变变量的类型是不可改变的。</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-常量&quot;&gt;1. 常量&lt;/h2&gt;
&lt;p&gt;声明常量使用的关键字是 &lt;code&gt;const&lt;/code&gt;。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;在声明常量时，必须注明值的类型。&lt;/li&gt;
&lt;li&gt;常量可以在任何作用域中声明，包括全局作用域。常量在它的作用域中，整个程序生命周期中</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust构建工具和包管理器Cargo</title>
    <link href="https://blueheart0621.github.io/2021/04/18/Rust/Rust%E6%9E%84%E5%BB%BA%E5%B7%A5%E5%85%B7%E5%92%8C%E5%8C%85%E7%AE%A1%E7%90%86%E5%99%A8Cargo/"/>
    <id>https://blueheart0621.github.io/2021/04/18/Rust/Rust%E6%9E%84%E5%BB%BA%E5%B7%A5%E5%85%B7%E5%92%8C%E5%8C%85%E7%AE%A1%E7%90%86%E5%99%A8Cargo/</id>
    <published>2021-04-18T13:24:34.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-简介">1. 简介</h2><p>Cargo 是 Rust 的构建系统和包管理器，大多数 Rustacean 们使用 Cargo 来管理他们的 Rust 项目，因为它可以处理很多任务，比如构建代码、下载依赖库（在 Rust 中称为「crate」）并编译这些库。</p><h2 id="2-使用-Cargo">2. 使用 Cargo</h2><h3 id="2-1-创建项目">2.1 创建项目</h3><p>Cargo 工具创建项目的基本语法格式如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">cargo new &lt;project&gt;<br></code></pre></td></tr></table></figure><p>运行上述命令后，会创建一个名为 <code>&lt;project&gt;</code> 的目录，并初始化该目录为一个仓库，其目录结构如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">.<br>├── .git<br>├── .gitignore<br>├── Cargo.toml<br>└── src<br>    └── main.rs<br></code></pre></td></tr></table></figure><ul><li><code>Cargo.toml</code> 为当前项目的 Cargo 配置文件，采用的是 <a href="https://github.com/toml-lang/toml">TOML</a> 文件格式。</li></ul><h3 id="2-2-构建项目">2.2 构建项目</h3><p>Cargo 工具构建项目的基本语法格式如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 在项目目录下执行</span><br>cargo build<br></code></pre></td></tr></table></figure><p>运行上述构建命令后，会在项目目录下生成一个 <code>target</code> 目录，该目录下的 <code>debug</code> 目录中包含了一些编译文件和生成的可执行文件等信息。</p><p>【注】当项目最终准备好发布时，可以使用 <code>cargo build --release</code> 来优化编译项目。这会在 <code>target/release</code> 下而不是 <code>target/debug</code> 下生成可执行文件。</p><blockquote><p>这些优化可以让 Rust 代码运行的更快，不过启用这些优化也需要消耗更长的编译时间。这也就是为什么会有两种不同的配置：一种是为了开发，你需要经常快速重新构建；另一种是为用户构建最终程序，它们不会经常重新构建，并且希望程序运行得越快越好。</p></blockquote><h3 id="2-3-运行项目">2.3 运行项目</h3><p>Cargo 工具运行项目的基本语法格式如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 在项目目录下执行</span><br>cargo run<br></code></pre></td></tr></table></figure><p>运行上述运行命令后，会运行构建过程生成的可执行文件 <code>./target/debug/&lt;project&gt;</code> 。</p><p>【注】如果没有构建项目直接运行 <code>cargo run</code>，Cargo 会先自动构建项目然后运行程序。</p><h3 id="2-4-检查代码">2.4 检查代码</h3><p>Cargo 工具还提供了快速检查项目代码确保其可以编译的功能，其基本语法格式如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 在项目目录下执行</span><br>cargo check<br></code></pre></td></tr></table></figure><p><code>cargo check</code> 比 <code>cargo build</code> 快得多，因为它省略了生成可执行文件的步骤。因此在编写代码时只是想持续的进行代码检查，<code>cargo check</code> 是一个不错的选择。</p><h3 id="2-5-查看文档">2.5 查看文档</h3><p>当我们在调用依赖库（crate）时，我们不知道应该从 crate 中调用那个方法，crate 的使用说明位于其文档中，所以我们需要查阅相关文档。而 Cargo 有一个很棒的功能是：运行 <code>cargo doc --open</code> 命令来构建所有本地依赖提供的文档，并在浏览器中打开。</p><h2 id="附录">附录</h2><p>关于 Cargo 工具更详细的介绍参考其官方文档：</p><ul><li><a href="https://doc.rust-lang.org/cargo/index.html">Rust 构建工具和包管理器：Cargo 使用文档</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-简介&quot;&gt;1. 简介&lt;/h2&gt;
&lt;p&gt;Cargo 是 Rust 的构建系统和包管理器，大多数 Rustacean 们使用 Cargo 来管理他们的 Rust 项目，因为它可以处理很多任务，比如构建代码、下载依赖库（在 Rust 中称为「crate」）并编译这些库</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust代码格式</title>
    <link href="https://blueheart0621.github.io/2021/04/18/Rust/Rust%E4%BB%A3%E7%A0%81%E6%A0%BC%E5%BC%8F/"/>
    <id>https://blueheart0621.github.io/2021/04/18/Rust/Rust%E4%BB%A3%E7%A0%81%E6%A0%BC%E5%BC%8F/</id>
    <published>2021-04-18T13:16:44.000Z</published>
    <updated>2021-04-28T03:45:37.096Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-代码格式">1. 代码格式</h2><ul><li>函数体需要用花括号包裹，一般左花括号与函数声明置于同一行并以空格分隔。</li><li>Rust 缩进风格为 4 个空格，而不是 1 个水平制表符。</li><li>Rust 代码中的函数和变量名使用 <a href="/2021/04/19/Programming/%E4%BB%A3%E7%A0%81%E9%A3%8E%E6%A0%BC%E8%A7%84%E8%8C%83">snake case</a> 规范风格。</li></ul><h2 id="2-自动格式化">2. 自动格式化</h2><p>Rust 开发了一个叫做 <code>rustfmt</code> 的自动格式化工具，能够将用户编写的代码自动调整为一种标准风格。最新版的 Rust 中已经包含了该工具。详细使用参见命令手册：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">rustfmt --<span class="hljs-built_in">help</span><br></code></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-代码格式&quot;&gt;1. 代码格式&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;函数体需要用花括号包裹，一般左花括号与函数声明置于同一行并以空格分隔。&lt;/li&gt;
&lt;li&gt;Rust 缩进风格为 4 个空格，而不是 1 个水平制表符。&lt;/li&gt;
&lt;li&gt;Rust 代码中的函数和变量名使用</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Rust学习资料汇总</title>
    <link href="https://blueheart0621.github.io/2021/04/18/Rust/Rust%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/"/>
    <id>https://blueheart0621.github.io/2021/04/18/Rust/Rust%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/</id>
    <published>2021-04-18T12:09:50.000Z</published>
    <updated>2021-04-28T03:45:37.099Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-学习资料">1. 学习资料</h2><h3 id="1-1-在线学习资料">1.1 在线学习资料</h3><ul><li><a href="https://www.rust-lang.org/zh-CN/learn">Rust 官方资料集合</a></li><li><a href="https://doc.rust-lang.org/book/">Rust 程序设计语言英文版</a></li><li><a href="https://kaisery.github.io/trpl-zh-cn/">Rust 程序设计语言中文版</a></li><li><a href="https://rust-lang.github.io/rustup/">Rust 安装和版本管理工具：Rustup 使用文档</a></li><li><a href="https://doc.rust-lang.org/cargo/index.html">Rust 构建工具和包管理器：Cargo 使用文档</a></li><li><a href="https://doc.rust-lang.org/std/index.html">Rust API 文档</a></li></ul><h3 id="1-2-本地学习资料">1.2 本地学习资料</h3><ul><li>运行 <code>rustup doc</code> 在浏览器查看本地文档。</li></ul><h2 id="2-学习论坛">2. 学习论坛</h2><ul><li><a href="https://discord.gg/rust-lang">Rust 官方 Discord 群聊</a></li><li><a href="https://rustcc.cn/">Rust 中文社区</a></li><li><a href="https://users.rust-lang.org/latest">Rust 用户论坛</a></li><li><a href="https://internals.rust-lang.org/">Rust 国际论坛</a></li><li><a href="https://stackoverflow.com/questions/tagged/rust">stack overflow</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-学习资料&quot;&gt;1. 学习资料&lt;/h2&gt;
&lt;h3 id=&quot;1-1-在线学习资料&quot;&gt;1.1 在线学习资料&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://www.rust-lang.org/zh-CN/learn&quot;&gt;Rust 官方资料集合&lt;/a&gt;&lt;/l</summary>
      
    
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/categories/Rust/"/>
    
    
    <category term="Rust" scheme="https://blueheart0621.github.io/tags/Rust/"/>
    
  </entry>
  
  <entry>
    <title>Vue学习资料汇总</title>
    <link href="https://blueheart0621.github.io/2021/04/17/JavaScript/Vue/Vue%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/"/>
    <id>https://blueheart0621.github.io/2021/04/17/JavaScript/Vue/Vue%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/</id>
    <published>2021-04-17T14:08:03.000Z</published>
    <updated>2021-04-28T03:45:37.086Z</updated>
    
    <content type="html"><![CDATA[<ul><li><a href="https://cn.vuejs.org/">官方手册</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://cn.vuejs.org/&quot;&gt;官方手册&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</summary>
      
    
    
    
    <category term="JavaScript" scheme="https://blueheart0621.github.io/categories/JavaScript/"/>
    
    <category term="Vue" scheme="https://blueheart0621.github.io/categories/JavaScript/Vue/"/>
    
    
    <category term="JavaScript" scheme="https://blueheart0621.github.io/tags/JavaScript/"/>
    
    <category term="Vue" scheme="https://blueheart0621.github.io/tags/Vue/"/>
    
  </entry>
  
  <entry>
    <title>Manjaro21.0系统报错</title>
    <link href="https://blueheart0621.github.io/2021/04/17/Linux/Manjaro/Manjaro21.0%E7%B3%BB%E7%BB%9F%E6%8A%A5%E9%94%99/"/>
    <id>https://blueheart0621.github.io/2021/04/17/Linux/Manjaro/Manjaro21.0%E7%B3%BB%E7%BB%9F%E6%8A%A5%E9%94%99/</id>
    <published>2021-04-17T14:00:46.000Z</published>
    <updated>2021-04-28T03:45:37.119Z</updated>
    
    <content type="html"><![CDATA[<h3 id="1-ENOSPC-System-limit-for-number-of-file-watchers-reached">1. ENOSPC: System limit for number of file watchers reached</h3><ul><li>问题：系统对文件监控的数量已经达到限制数量了。</li><li>解决：<ul><li>修改 <code>/etc/sysctl.conf</code> 文件，添加以下代码：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">fs.inotify.max_user_watches=52428800<br></code></pre></td></tr></table></figure></li><li>然后刷新配置：<figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash">sudo sysctl -p<br></code></pre></td></tr></table></figure></li></ul></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h3 id=&quot;1-ENOSPC-System-limit-for-number-of-file-watchers-reached&quot;&gt;1. ENOSPC: System limit for number of file watchers reached&lt;/h3&gt;
&lt;ul&gt;
&lt;li</summary>
      
    
    
    
    <category term="Linux" scheme="https://blueheart0621.github.io/categories/Linux/"/>
    
    <category term="Manjaro" scheme="https://blueheart0621.github.io/categories/Linux/Manjaro/"/>
    
    
    <category term="Linux" scheme="https://blueheart0621.github.io/tags/Linux/"/>
    
    <category term="Manjaro" scheme="https://blueheart0621.github.io/tags/Manjaro/"/>
    
  </entry>
  
  <entry>
    <title>Linux下更新Deepin-Wine5软件</title>
    <link href="https://blueheart0621.github.io/2021/04/17/Linux/Linux%E4%B8%8B%E6%9B%B4%E6%96%B0Deepin-Wine5%E8%BD%AF%E4%BB%B6/"/>
    <id>https://blueheart0621.github.io/2021/04/17/Linux/Linux%E4%B8%8B%E6%9B%B4%E6%96%B0Deepin-Wine5%E8%BD%AF%E4%BB%B6/</id>
    <published>2021-04-17T08:40:15.000Z</published>
    <updated>2021-04-28T03:45:37.116Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-简介">1. 简介</h2><p>Deepin-Wine 已经升级到第 5 版，对国内一些 Windows 应用兼容性也更好，希望深度越做越好（<s>虽然 Linux 原版要更好，但奈何大多数国内软件厂家都不做 Linux 版应用</s>）。</p><h2 id="2-更新">2. 更新</h2><p>假设 Linux 系统上已经安装了 <code>deepin-wine5</code> 和一些 wine 版的软件，比如 Tim 和 WeChat。如果要更新这些应用（直接在软件里更新是显然不成的），需要先到官网下载对应安装包，然后运行下面代码就能够实现 wine 版软件的更新：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><code class="hljs bash">env WINEPREFIX=<span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.deepinwine/Spark-TIM/&quot;</span> deepin-wine5 TIM.xxx.exe<br>env WINEPREFIX=<span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/.deepinwine/Deepin-WeChat/&quot;</span> deepin-wine5 WeChatSetup.exe<br></code></pre></td></tr></table></figure><p>其中，<code>WINEPREFIX</code> 是用来指定原 wine 版应用的路径的。</p><h2 id="附录">附录</h2><p>参考资料：</p><ul><li><a href="https://www.loafing.cn/posts/Hello-Deepin-my-old-friend.html">「养老」重装 Deepin OS + 优化记录（备忘手册）</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-简介&quot;&gt;1. 简介&lt;/h2&gt;
&lt;p&gt;Deepin-Wine 已经升级到第 5 版，对国内一些 Windows 应用兼容性也更好，希望深度越做越好（&lt;s&gt;虽然 Linux 原版要更好，但奈何大多数国内软件厂家都不做 Linux 版应用&lt;/s&gt;）。&lt;/p&gt;
&lt;h2</summary>
      
    
    
    
    <category term="Linux" scheme="https://blueheart0621.github.io/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://blueheart0621.github.io/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>LaTeX学习资料汇总</title>
    <link href="https://blueheart0621.github.io/2021/04/17/TeX/LaTeX/LaTeX%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/"/>
    <id>https://blueheart0621.github.io/2021/04/17/TeX/LaTeX/LaTeX%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/</id>
    <published>2021-04-17T08:20:27.000Z</published>
    <updated>2021-04-28T03:45:37.089Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-学习资料">1. 学习资料</h2><h3 id="1-1-在线学习资料">1.1 在线学习资料</h3><ul><li><a href="https://www.learnlatex.org/">Learn LaTeX.org</a></li><li><a href="http://www.ctex.org/HomePage">CTex.org</a></li></ul><h3 id="1-2-本地学习资料">1.2 本地学习资料</h3><ul><li><code>texdoc</code> 工具</li></ul><h2 id="2-学习论坛">2. 学习论坛</h2><ul><li><a href="https://tex.stackexchange.com/">tex stack exchange</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-学习资料&quot;&gt;1. 学习资料&lt;/h2&gt;
&lt;h3 id=&quot;1-1-在线学习资料&quot;&gt;1.1 在线学习资料&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://www.learnlatex.org/&quot;&gt;Learn LaTeX.org&lt;/a&gt;&lt;/li&gt;
&lt;li</summary>
      
    
    
    
    <category term="TeX" scheme="https://blueheart0621.github.io/categories/TeX/"/>
    
    <category term="LaTeX" scheme="https://blueheart0621.github.io/categories/TeX/LaTeX/"/>
    
    
    <category term="TeX" scheme="https://blueheart0621.github.io/tags/TeX/"/>
    
    <category term="LaTeX" scheme="https://blueheart0621.github.io/tags/LaTeX/"/>
    
  </entry>
  
  <entry>
    <title>NodeJS学习资料汇总</title>
    <link href="https://blueheart0621.github.io/2021/04/17/JavaScript/NodeJS/NodeJS%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/"/>
    <id>https://blueheart0621.github.io/2021/04/17/JavaScript/NodeJS/NodeJS%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/</id>
    <published>2021-04-17T08:18:26.000Z</published>
    <updated>2021-04-28T03:45:37.086Z</updated>
    
    <content type="html"><![CDATA[<ul><li><a href="http://nodejs.cn/api/">中文官方文档</a></li><li><a href="">英文官方文档</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;http://nodejs.cn/api/&quot;&gt;中文官方文档&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;&quot;&gt;英文官方文档&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</summary>
      
    
    
    
    <category term="JavaScript" scheme="https://blueheart0621.github.io/categories/JavaScript/"/>
    
    <category term="NodeJS" scheme="https://blueheart0621.github.io/categories/JavaScript/NodeJS/"/>
    
    
    <category term="JavaScript" scheme="https://blueheart0621.github.io/tags/JavaScript/"/>
    
    <category term="NodeJS" scheme="https://blueheart0621.github.io/tags/NodeJS/"/>
    
  </entry>
  
  <entry>
    <title>MarkDown学习资料汇总</title>
    <link href="https://blueheart0621.github.io/2021/04/17/MarkDown/MarkDown%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/"/>
    <id>https://blueheart0621.github.io/2021/04/17/MarkDown/MarkDown%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/</id>
    <published>2021-04-17T05:08:01.000Z</published>
    <updated>2021-04-28T03:45:37.082Z</updated>
    
    <content type="html"><![CDATA[<ul><li><a href="http://itmyhome.com/markdown/index.html">Learning-Markdown (Markdown 入门参考)</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;http://itmyhome.com/markdown/index.html&quot;&gt;Learning-Markdown (Markdown 入门参考)&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
</summary>
      
    
    
    
    <category term="MarkDown" scheme="https://blueheart0621.github.io/categories/MarkDown/"/>
    
    
    <category term="MarkDown" scheme="https://blueheart0621.github.io/tags/MarkDown/"/>
    
  </entry>
  
  <entry>
    <title>Linux学习资料汇总</title>
    <link href="https://blueheart0621.github.io/2021/04/17/Linux/Linux%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/"/>
    <id>https://blueheart0621.github.io/2021/04/17/Linux/Linux%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%96%99%E6%B1%87%E6%80%BB/</id>
    <published>2021-04-17T04:34:42.000Z</published>
    <updated>2021-04-28T03:45:37.116Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-在线学习资料">1. 在线学习资料</h2><h3 id="1-1-Bash-Shell">1.1 Bash Shell</h3><ul><li><a href="https://www.gnu.org/software/bash/manual/bash.html">bash-manual</a></li><li><a href="https://tldp.org/LDP/abs/html/index.html">Advanced Bash-Scripting Guide</a></li></ul><h3 id="1-2-鸟哥的-Linux-私房菜">1.2 鸟哥的 Linux 私房菜</h3><ul><li><a href="http://cn.linux.vbird.org/">鸟哥的 Linux 私房菜</a></li></ul><h2 id="2-本地学习资料">2. 本地学习资料</h2><ul><li><code>help</code> 命令</li><li><code>man</code> 手册</li><li><code>info</code> 手册</li></ul><h2 id="3-学习论坛">3. 学习论坛</h2><ul><li><a href="https://stackoverflow.com/">stack overflow</a></li><li><a href="https://unix.stackexchange.com/">unix stack exchange</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-在线学习资料&quot;&gt;1. 在线学习资料&lt;/h2&gt;
&lt;h3 id=&quot;1-1-Bash-Shell&quot;&gt;1.1 Bash Shell&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://www.gnu.org/software/bash/manual/bash</summary>
      
    
    
    
    <category term="Linux" scheme="https://blueheart0621.github.io/categories/Linux/"/>
    
    
    <category term="Linux" scheme="https://blueheart0621.github.io/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>LinuxShell自动交互</title>
    <link href="https://blueheart0621.github.io/2021/04/16/Linux/Shell/LinuxShell%E8%87%AA%E5%8A%A8%E4%BA%A4%E4%BA%92/"/>
    <id>https://blueheart0621.github.io/2021/04/16/Linux/Shell/LinuxShell%E8%87%AA%E5%8A%A8%E4%BA%A4%E4%BA%92/</id>
    <published>2021-04-16T12:34:12.000Z</published>
    <updated>2021-04-28T03:45:37.122Z</updated>
    
    <content type="html"><![CDATA[<h2 id="1-简介">1. 简介</h2><p>Linux Shell 的自动交互在很多场景下会使用到，比如我们需要批量运行一些需要输入参数进行交互的命令时，此时 Shell 的自动交互就能将我们从重复无聊的手动交互中解放出来。</p><h2 id="2-方法">2. 方法</h2><p>以下我们通过 <code>ssh-keygen</code> 命令生成密钥来举例说明各个方法的使用。</p><h3 id="2-1-内联输入重定向符">2.1 内联输入重定向符</h3><p>内联输入重定向符是一种输入<a href="/2020/05/26/Linux/Shell/LinuxShell%E9%87%8D%E5%AE%9A%E5%90%91%E5%92%8C%E7%AE%A1%E9%81%93">重定向符</a>，该输入重定向符用于从命令行中指定用于输入重定向的数据，因此可以用它来实现 Shell 的自动交互。</p><ul><li>需要注意的是，在以下方式中，两个「标记符」<code>EOF</code> （也可以设为其他的字符串）之间的每一行为直接对应每一条交互输入。因此，给出的参数前后不能有多余的空格和 Tab，否则也会被当作输入参数的一部分。</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br><br>ssh-keygen -t rsa -C <span class="hljs-string">&quot;example@163.com&quot;</span> &lt;&lt; <span class="hljs-string">EOF</span><br><span class="hljs-string">$HOME/Tmp/example</span><br><span class="hljs-string"></span><br><span class="hljs-string"></span><br><span class="hljs-string">EOF</span><br></code></pre></td></tr></table></figure><ul><li>若需要消除给出的参数前面多余的 Tab，特别是当 <code>ssh-keygen</code> 处于多层缩进区域时，如果顶行写会导致脚本不美观。此时我们可以使用 <code>&lt;&lt;-</code> 来忽略每行参数前面的前导 Tab（注意只能消除 Tab，不能消除空格）。</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br><br>ssh-keygen -t rsa -C <span class="hljs-string">&quot;example@163.com&quot;</span> &lt;&lt;-<span class="hljs-string">EOF</span><br><span class="hljs-string">$HOME/Tmp/example</span><br><span class="hljs-string"></span><br><span class="hljs-string"></span><br><span class="hljs-string">EOF</span><br></code></pre></td></tr></table></figure><p>【注】关于「内联输入重定向符」更多详细介绍参见<a href="/2020/05/26/Linux/Shell/LinuxShell%E9%87%8D%E5%AE%9A%E5%90%91%E5%92%8C%E7%AE%A1%E9%81%93">LinuxShell重定向和管道</a>。</p><h3 id="2-2-管道符">2.2 管道符</h3><p>除了使用内联输入重定向符，也可以使用<a href="/2020/05/26/Linux/Shell/LinuxShell%E9%87%8D%E5%AE%9A%E5%90%91%E5%92%8C%E7%AE%A1%E9%81%93">管道符</a>来实现 Shell 自动交互。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br><br><span class="hljs-built_in">echo</span> -e <span class="hljs-string">&quot;<span class="hljs-variable">$HOME</span>/Tmp/example\n\n\n&quot;</span> | ssh-keygen -t rsa -C <span class="hljs-string">&quot;example@163.com&quot;</span><br></code></pre></td></tr></table></figure><h3 id="2-3-expect-命令">2.3 <code>expect</code> 命令</h3><p><code>expect</code> 是一个专门用来实现 Shell 自动化交互的工具，有些 Linux 发行版可能没有预装，需要自行安装。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/usr/bin/expect</span><br><br>spawn ssh-keygen -t rsa -C <span class="hljs-string">&quot;example@163.com&quot;</span><br>expect <span class="hljs-string">&quot;Enter file in which to save the key&quot;</span><br>send <span class="hljs-string">&quot;<span class="hljs-variable">$env</span>(HOME)/Tmp/example\n&quot;</span><br>expect <span class="hljs-string">&quot;Enter passphrase&quot;</span><br>send <span class="hljs-string">&quot;\n&quot;</span><br>expect <span class="hljs-string">&quot;Enter same passphrase again&quot;</span><br>send <span class="hljs-string">&quot;\n&quot;</span><br>expect off<br></code></pre></td></tr></table></figure><ul><li><code>expect</code> 类似于 <code>bash</code> 作为一个解释器，因此使用 <code>expect</code> 时需要在脚本开头指定 <code>#!/usr/bin/expect</code> 而不是 <code>#!/bin/bash</code>。</li><li><code>expect</code> 下定义的变量称为 TCL 变量，它和 Shell 下的变量是不互通的。因此 <code>expect</code> 不能直接使用 Shell 下的环境变量，若要使用 Shell 下 <code>ENV</code> 环境变量则需使用 <code>$env(ENV)</code> 语法。</li></ul><p>在 <code>expect</code> 脚本中：</p><ul><li><code>spawn</code> 后面指定要实现自动交互的命令。</li><li><code>expect</code> 用来等待要识别的消息。</li><li><code>send</code> 用来发送输入参数。</li><li><code>expect off</code> 用来指明自动交互结束。</li></ul><p>【注】更多关于 <code>expect</code> 工具的详解参见官方手册 <code>man expect</code> 。</p><h2 id="附录">附录</h2><p>更多详细介绍参考以下资料：</p><ul><li><a href="https://unix.stackexchange.com/questions/410544/send-from-variable-in-usr-bin-expect-f">send from variable in #!/usr/bin/expect -f</a></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h2 id=&quot;1-简介&quot;&gt;1. 简介&lt;/h2&gt;
&lt;p&gt;Linux Shell 的自动交互在很多场景下会使用到，比如我们需要批量运行一些需要输入参数进行交互的命令时，此时 Shell 的自动交互就能将我们从重复无聊的手动交互中解放出来。&lt;/p&gt;
&lt;h2 id=&quot;2-方法&quot;&gt;2. </summary>
      
    
    
    
    <category term="Linux" scheme="https://blueheart0621.github.io/categories/Linux/"/>
    
    <category term="Shell" scheme="https://blueheart0621.github.io/categories/Linux/Shell/"/>
    
    
    <category term="Linux" scheme="https://blueheart0621.github.io/tags/Linux/"/>
    
    <category term="Shell" scheme="https://blueheart0621.github.io/tags/Shell/"/>
    
  </entry>
  
</feed>
